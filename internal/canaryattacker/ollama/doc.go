// Package ollama implements the fixed-loopback Ollama transport used by the
// bounded CanaryAttacker planner. It does not inherit proxy settings and cannot
// connect to a non-loopback endpoint.
package ollama
