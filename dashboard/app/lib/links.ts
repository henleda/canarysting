// Home-wall links inherit color and drop the underline so the signed-off look is unchanged — only a pointer cursor appears.
export const WALL_LINK = { color: 'inherit', textDecoration: 'none' } as const;
