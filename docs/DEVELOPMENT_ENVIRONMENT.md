# CanarySting Development Environment

This document defines the supported development split between the Mac workstation and the DGX Spark integration node. It records capabilities and risks, never credentials. `AGENTS.md` remains authoritative for architecture and safety; `docs/DEVELOPMENT_PLAN.md` controls task order and completion.

## Environment model

The Mac is the canonical source and build environment. Source editing, Go development, unit tests, static analysis, generated-code checks, cross-compilation, CI-equivalent checks, and Git operations happen here. Do not make the DGX the canonical checkout and do not edit source on it unless the user explicitly requests that exception.

The DGX Spark is the canonical Linux ARM64 integration environment and Kubernetes reference laboratory. It is used for K3s, Cilium coexistence, Envoy integration, eBPF load/attach, socket-cookie and cgroup behavior, kernel enforcement, workload identity, NetworkPolicy, optional operator/DaemonSet deployment, and end-to-end validation. It is also CanaryPlatform's initial correlation laboratory: CanaryView will compare observations from the local Kubernetes/Cilium/Hubble/Envoy/kernel/CanarySting stack against controlled CanaryAttacker ground truth there. Synthetic lab evidence remains execution output governed by `docs/CANARYVIEW_STORAGE_AND_RETENTION.md`; it is not canonical source and must remain isolated from production baselines and customer models. This laboratory role does not make Kubernetes, the DGX, or customer-side software prerequisites for CanaryView SaaS.

The build/execution boundary is deliberate. The Mac owns source, builds, tests, manifests, and Git history. The DGX executes checksum-identified ARM64 artifacts and declarative lab fixtures from repository scripts. Do not create a canonical source checkout or edit source directly on the DGX; a remote staging directory is an execution input, not a development workspace.

Preferred flow:

1. Select one task and validation tier from `docs/DEVELOPMENT_PLAN.md`.
2. Edit and complete Tier A validation on the Mac.
3. Cross-build only named Linux/ARM64 artifacts on the Mac with `scripts/dgx/build.sh`.
4. Run `scripts/dgx/check.sh` before any remote mutation.
5. Transfer only named artifacts to a run-specific directory beneath `/var/tmp/canarysting/`.
6. Execute a repository script; collect logs and before/after state.
7. Remove only resources bearing the run identifier, then verify K3s/Cilium/connectivity and BPF state.
8. Record evidence in `docs/DEVELOPMENT_PLAN.md`.

Product deployment profiles are separate from this development split. Profile 1 runs CanaryView through direct read-only integrations with no new customer-side data-plane software. Profile 2 adds one optional Site Gateway where private access, buffering, residency, preprocessing, or controlled delivery requires it. Profiles 3 and 4 add selected managed assets and local response; Kubernetes/eBPF are reference implementations for relevant cases, not universal requirements.

## Mac workstation

Verified 2026-08-24:

- macOS 26.6 on ARM64.
- Go 1.25.3 (`go.mod` remains the version authority for builds).
- Git, Make, Apple Clang, `protoc`, Docker, Node, and npm are present.
- This repository is the canonical checkout.

Mac builds must not silently depend on DGX state. eBPF C compilation and privileged runtime checks are Linux validations even when Go userspace cross-compiles successfully on macOS.

## DGX Spark

Connection uses the existing SSH alias `falcon1`; scripts must not embed an address, password, key, token, or kubeconfig. Expected hostname is `spark-5343`. A hostname or architecture mismatch is a hard stop.

Read-only verification on 2026-08-24 found:

- Ubuntu 24.04.4 LTS, ARM64, NVIDIA kernel `6.17.0-1031-nvidia`.
- 20 CPU cores, about 121 GiB memory, and a 3.7 TB root NVMe volume.
- K3s `v1.36.3+k3s1` active; the single node was Ready; K3s uses containerd.
- Cilium CLI `v0.19.7`; Cilium/operator `1.19.5` and Cilium Envoy were healthy (1/1 each). Hubble Relay and ClusterMesh were disabled.
- cgroup v2, bpffs, readable kernel BTF, and BPF JIT enabled.
- kernel support for `sock_ops` and `cgroup_skb`.
- root cgroup attachments were Cilium-owned multi-attach programs; no CanarySting attachment was observed.
- Docker, `bpftool`, K3s, kubectl, Cilium CLI, and Ollama were present. Go and Clang were absent.
- The local Ollama model `qwen3-coder:30b-a3b-q8_0` was observed in an earlier environment review. Its current presence, version, loopback binding, and readiness were not re-verified by this architecture bootstrap and must be checked read-only before a future CanaryAttacker task depends on it.
- no CanarySting Kubernetes resources, matching test pods, NetworkPolicies, CiliumNetworkPolicies, process, or named BPF program/map was found.
- an earlier interrupted validation left `/tmp/canarysting-m15.Nq0THn` and the Docker image `ubuntu:24.04`. These are historical test artifacts, not a source checkout or product deployment; remove them only through an explicitly authorized cleanup task.

Facts above are observations, not permanent assumptions. Re-run `scripts/dgx/check.sh` before depending on them. The first Cilium CLI call must set `KUBECONFIG=/etc/rancher/k3s/k3s.yaml`; without it, the CLI falls back to an unusable localhost configuration.

## DGX harness contract

The repository-owned entry points are:

- `check.sh` — read-only host, cluster, Cilium, BPF, policy, and CanarySting inventory.
- `build.sh` — deterministic, offline Mac Linux/ARM64 builds from an allowlisted target catalog. It requires an explicit destination outside the repository whose parent already exists, refuses to overwrite it, separates `product/` from `test/`, and writes `manifest.tsv` plus `SHA256SUMS` with source-state evidence. Here `product/` means a deployable-component artifact rather than a test diagnostic; it does not assert production maturity. Use `--list` to inspect the catalog; for example: `scripts/dgx/build.sh --output-dir /tmp/canarysting-build-m1b2 --target cookiespike`. The local Go toolchain and module cache must already satisfy the build; the script never downloads or installs them.
- `build_test.sh` — focused local regression proof for the build harness. It verifies input refusals, product/test separation, ARM64 ELF output, manifest checksums, overwrite protection, and byte-identical repeated builds.
- `copy.sh` — checksum-verified transfer of a `build.sh` output directory to the fixed `falcon1` alias. It requires a strict run ID, validates the exact local manifest/allowlist/filesystem inventory, verifies `spark-5343` and ARM64 before mutation, uploads to `/var/tmp/canarysting/.incoming-<run-id>`, repeats the validation remotely, and atomically publishes `/var/tmp/canarysting/<run-id>`. Existing stages are never overwritten, pre-finalization failures remove only their exact incoming directory, `--dry-run` performs no SSH access, and `--verify-only` rechecks a published stage read-only. Successful stages are retained explicitly for the named downstream run until the cleanup workflow removes them.
- `copy_test.sh` — focused local negative coverage for transfer inputs, including corrupt or missing artifacts, unexpected files, symlinks, unsafe manifest paths, malformed inventories, and invalid run IDs.
- `run.sh` — deterministic execution of one fixed profile from a verified immutable artifact stage. It accepts no arbitrary command, argument, timeout, path, or privilege. The initial `engine-selfcheck` and `engine-timeout-probe` profiles are unprivileged, in-memory, listener-free, database-free, and BPF-free. Each profile has a hardcoded timeout and expected outcome. Bounded logs plus an atomic `result.tsv` are written to the explicit sibling `/var/tmp/canarysting/execution-<run-id>` directory; an existing evidence directory is never overwritten, and the script verifies no artifact process remains.
- `run_test.sh` — focused local coverage for the execution profile catalog, dry-run contract, input validation, and arbitrary-command/argument/privilege refusals.
- `collect.sh` — fixed-schema collection of one verified M1B run. It accepts only a strict run ID and a new absolute output directory outside the repository; verifies the remote artifact stage and execution evidence; bounds all files; refuses credential-like material before transfer; rechecks transfer digests; runs the read-only DGX state checker; removes ANSI presentation codes; and atomically publishes mode-0600 files beneath a mode-0700 directory with a manifest and SHA-256 inventory. It never accepts an arbitrary remote path and never reads kubeconfig or Kubernetes Secret contents. `--dry-run` is local-only; `--fixture-dir` exercises the same local schema/sensitivity checks without SSH.
- `collect_test.sh` — focused collection coverage for schema/checksum linkage, exact inventory, output modes, retention/model-use metadata, overwrite and arbitrary-path refusal, wrong run identity, manifest drift, symlinks, and negative bearer/kubeconfig-secret fixtures.
- `cleanup.sh` — exact-run, idempotent cleanup for the generic M1B harness. A required strict run ID resolves only `/var/tmp/canarysting/.incoming-<run-id>`, `/var/tmp/canarysting/<run-id>`, and `/var/tmp/canarysting/execution-<run-id>`. Before removing any candidate, the remote half verifies the expected host/architecture, root and entry ownership, non-symlink types, fixed artifact/evidence schemas, published-stage checksums, evidence run identity, and absence of a process executing from the stage. `--inspect` validates remotely without mutation; it recognizes and reports an exact safe `model-load-owned` marker so scenario-specific recovery can inspect the stage. `--dry-run` does not access the DGX. A normal cleanup proves each exact path absent and runs the full read-only DGX checker afterward. Repeating it against absent state is a successful no-op. Generic mutating cleanup refuses an active marker so it cannot erase the M2C.4 unload recovery path; only the scenario-specific attacker-loop cleanup may unload the exact pinned model, retain that marker until an independent zero-loaded inspection succeeds under the same exclusive model lock, remove it, and then delegate here.
- `cleanup_test.sh` — focused local coverage for exact path derivation, excluded state, strict run IDs, mutually exclusive modes, and refusal of arbitrary paths, broad cleanup, historical cleanup, or privilege escalation.
- `cookiespike.sh` — the M1C observe-only socket-cookie proof. It accepts only a strict run ID and fixed run/inspect/cleanup modes, verifies the checksum-identified `test/cookiespike` stage, then refuses privileged snapshot work unless `/var/tmp` is a root-owned mode-1777 directory. The privileged helper opens the staged artifact once, copies it into the exact root-owned mode-0700 `/var/tmp/canarysting-privileged/cookiespike-<run-id>` directory, verifies the copied size and SHA-256 against the staged manifest, locks the copy to mode 0500, and executes only that verified snapshot under a 15-second timeout in `/sys/fs/cgroup/canarysting-dgx/<run-id>`. The binary creates one loopback connection, rebuilds the Envoy remote-to-local tuple, resolves it through the production staleness guard, compares the nonzero result with `SO_COOKIE`, proves an absent tuple remains unattributable, and proves close-delete with an error-preserving map lookup. The harness observes `canary_sockops` live on the exact child and absent from its parent/root, compares complete program/map/link ID inventories and root cgroup attachments before/after, fails closed when any BPF inventory cannot be read, refuses pre-existing named CanarySting BPF/process state, and never loads enforcement. Stdout and stderr are each capped while being drained; discarded output makes the run fail without allowing either file to exceed 1 MiB. The fixed three-file evidence directory is bound to the verified artifact checksum and records privileged-snapshot verification, cleanup, and absence; it is validated for exact modes, byte counts, checksums, and required result fields before read-only inspection; inspection refuses interrupted quarantine residue. Cleanup removes only the run's empty child cgroup, root-owned privileged snapshot, M1C evidence, and verified artifact stage, and is idempotent. Its recovery path physically anchors the fixed owned, non-symlink harness root, validates the exact owned evidence directory and allowlisted regular files, atomically renames that directory with no-target-directory semantics into a run-scoped quarantine, enters it through an explicit `./` path, verifies the resulting physical working directory remains the fixed quarantine path, revalidates from inside, and deletes only relative allowlisted names with explicit failure propagation. Startup refuses a same-ID run while quarantine remains; cleanup safely drains an interrupted quarantine before any exact live evidence. Oversized or malformed contents remain recoverable, while `CDPATH`, ancestor, leaf, destination, or validation-to-entry substitution cannot change an external target or produce a false cleanup success.
- `enforcespike.sh` — the M1D precise containment/Cilium coexistence proof. It accepts only a strict run ID and fixed run/inspect/cleanup modes, pins every check/copy/cleanup operation to `falcon1`, captures the expected `test/enforcespike` size and SHA-256 from the verified stage manifest, and permits the root helper to execute only a descriptor-copied snapshot that matches that frozen identity. The fixed timeout runs in `/sys/fs/cgroup/canarysting-dgx/<run-id>`. The binary attaches the production sockops, cgroup-skb egress, and sock-release programs only to that child; creates control then target connections through one shared listener/destination; validates both distinct socket cookies against `SO_COOKIE`; proves both flows before enforcement; treats only the target as an explicit proof-fixture canary touch; programs only that cookie through `containment.Apply`; proves target drops and kernel counters while the same-destination control remains a verdict-map miss; then holds the target verdict while an atomically published marker/acknowledgement handshake proves K3s/node/Cilium health after traffic has exercised enforcement. It releases the target only after that acknowledgement and verifies that the same connection recovers. The harness observes all three programs live on the child and absent from parent/root, rechecks the attachments around the enforcement-active health sample, checks health again after detach, compares root attachments plus complete BPF program/map/link inventories before/after, and fails closed on command or inventory errors. Stdout and stderr are continuously drained through independent 1 MiB bounded captures; discarded output makes the proof fail without allowing either log to exceed the cap. Inspection revalidates the artifact stage, exact result schema, log sizes/digests, timestamp ordering, before/after inventory equality, cookie relationships, and unique proof markers. Runtime cleanup removes only its exact empty child cgroup and privileged snapshot. Evidence lives on the user runtime tmpfs, and a retrying transient systemd deletion timer is activated before any evidence directory is created, so both successful and partial runs remain bounded; reboot removes evidence earlier. Manual `--cleanup` atomically quarantines and deletes evidence below a descriptor-held root while the retrying timer remains available, verifies both directory unlinks, then cancels the exact timer, verifies its units are unloaded, and delegates the verified artifact stage to the generic cleanup workflow. It never changes Cilium or Kubernetes state.
- `dgxstackspike.sh` — the M2B.2 read-only normalization proof. It verifies the checksum-identified `test/dgxstackspike` artifact, captures bounded inventory from the existing CanarySting, engine-process, kernel/eBPF, Cilium Envoy, Kubernetes node, Cilium, and Hubble surfaces, and never applies or changes source state. Hubble or CanarySting absence remains useful partial source evidence rather than causing invented events. Each raw capture is capped at 1 MiB, hashed into an opaque federated reference and content checksum, then deleted before the evidence directory is atomically published. The artifact emits exactly one scoped schema-v3 synthetic partial observation per source with explicit provenance, confidence, lifecycle, and missing-evidence metadata. It retains no capture payload, source locator, request path/body/header, label/annotation, address, UID, canary value, feature value, or credential. Inspection revalidates the fixed three-file inventory, modes, sizes, source cardinality, checksums, synthetic/privacy markers, and raw-capture absence; the generic exact-run cleanup removes the evidence and artifact stage and performs the normal after-state check.
- `correlationspike.sh` — the M2B.3 unprivileged known-flow correlation proof. It verifies the checksum-identified `test/correlationspike` artifact, opens exactly two transient loopback TCP connections representing a proxy downstream/upstream pair, reads one real Linux `SO_COOKIE`, and immediately replaces addresses, request IDs, controls, and the cookie with run-scoped digests. The fixed proof validates exact socket-cookie, strong explicit-translation, strong OpenTelemetry-trace, weak tuple/time, missing-time rejection, equal-candidate ambiguity, and finite-bound behavior. It writes only eight fixed proof lines, empty stderr, and a result/checksum record below the generic execution directory; raw addresses, the cookie value, payloads, headers, credentials, and source captures are never emitted. It accepts no arbitrary command/path/timeout/privilege, makes no Kubernetes/Cilium/BPF/service/firewall/system change, and uses the generic exact-run cleanup for its stage and evidence.
- `tracespike.sh` — the combined M2B.4 trace-construction and M2B.5 operator-projection proof. It verifies the checksum-identified `test/tracespike` artifact and uses only minimized synthetic in-process records bound to the stable `m2b5-operator-conflict` scenario. It proves passive partial traces without canary touch, input-order-independent trace IDs, retained ambiguous candidates, evidence-cited joins, broken raw-reference visibility, hold/expiry behavior, exact-scope parent invalidation, refusal rather than truncation at hard bounds, and exact Go projection facts for the partial/conflicted operator workspace. It writes only nine fixed proof lines, empty stderr, and an exact result/checksum record below the generic execution directory. The Go proof does not claim browser click behavior; zero-interaction explanation and one-activation raw-reference access are Playwright evidence. The fixed 19-line result is bound to run, the exact stable scenario, source revision/state/tree, and artifact digest; its strict validator rejects missing, duplicate, reordered, unexpected, malformed, scenario/checksum-mismatched, unsuccessful, or expired/non-24-hour evidence. It accepts no arbitrary command/path/timeout/privilege. The proof artifact opens no sockets or changes source/Kubernetes/Cilium/BPF/service/firewall/system state; the wrapper uses only the normal SSH transport and generic exact-run cleanup for its stage and evidence.
- `attackerexecutorspike.sh` — the M2C.3 unprivileged closed-executor proof. It verifies the checksum-identified `test/attackerexecutorspike` artifact and executes the seven reviewed tools against one process-lifetime IPv4 loopback HTTP/TCP fixture. It proves intent-before-resolve/dial ordering, exact private target binding, proxy bypass, same-target link following, fixture-only credential resolution, redirect and DNS-set-change refusal, bounded response/cancellation/action behavior, audited denial of an unregistered shell tool, and listener shutdown. It runs no model and emits only eight fixed non-secret statements, empty stderr, and an exact 20-line result bound to run, scenario, source revision/state/tree, artifact checksum, fixed output, and a validated 24-hour expiry. It accepts no arbitrary command, path, target, scenario, timeout, privilege, credential, or payload. The artifact and wrapper make no source, Kubernetes, Cilium, BPF, service, firewall, container, package, or system change; generic exact-run cleanup removes only its verified stage and evidence.
- `attackerloopspike.sh` — the M2C.4 unprivileged live-Qwen proof. It completes or verifies the read-only DGX preflight before creating its lock file, then starts one remote supervisor that both owns the exclusive host-global `flock` in the DGX user's runtime directory and launches the reviewed proof as a dedicated process group. That same remote process holds the lock from before the first model-state inspection through proof-group termination, explicit unload, the independent zero-loaded postcheck, and recovery-marker retirement; a concurrent run fails closed instead of sharing model state. HUP/INT/TERM handling terminates and waits for the complete proof process group before the supervisor can release the lock. If the local non-PTY SSH client disappears without terminating its remote session, the surviving supervisor continues to own the lock until the bounded proof exits, so a detached proof cannot later unload a model outside its serialization boundary. Before run and after execution/inspection it requires the M2C.1 inspector to prove the exact `qwen3-coder:30b-a3b-q8_0` model ID `7b438a19895a`, loopback-only service/API ownership, safe inventory, synchronized time, at least one available GPU, at least 40 GiB available host memory, and zero loaded models. The checksum-built `test/attackerloopspike` opens one process-lifetime loopback HTTP fixture and calls only the fixed loopback Ollama API. Qwen sees one opaque zero-argument action handle; the coordinator retains the target, operation, policy, fixture, and action/token/time/byte budgets, and the M2C.3 executor commits exactly one intent and action before the deterministic `scenario_complete` stop. Immediately before the artifact may load Qwen, the wrapper writes a strict run-owned marker; cleanup unloads the pinned model only while that marker exists, retains the marker after the API acknowledges unload, and removes it only after an independent zero-loaded inspection succeeds under the same exclusive model lock. This preserves retry authority when Ollama acknowledges unload before the model is actually absent. `keep_alive=0` and a prompt-free, fixed-model `/api/generate` cleanup mode unload the run-owned model on normal, error, cancellation, and remote-shell signal paths. Redirect and transport failures are reduced to fixed diagnostics before reaching proof stderr. The wrapper emits no raw prompt, model prose, tool-call ID/name/arguments, response-controlled location/error, address, port, target, request, or response. It publishes only eight fixed assertions plus a strict 24-line result record and empty stderr, then performs exact and idempotent run cleanup—including an already-absent shared root—and the normal DGX health check. It has no Kubernetes, Cilium, BPF, firewall, service, package, source, or credential authority.
The M1D `enforcespike.sh` wrapper bounds SSH connection establishment to 12 seconds, uses strict host-key checking, and bounds an established but unresponsive session with 5-second keepalives and three missed replies. These transport bounds prevent its run/inspect/cleanup connection from consuming the overall CI job budget while preserving fail-closed proof semantics.

- `deploy.sh` — future declarative product deployment, distinct from test fixtures.

`deploy.sh` remains bootstrap scaffolding and currently fails closed. The generic cleanup intentionally excludes Kubernetes resources, BPF objects and attachments, containers and images, system configuration, product deployments, and historical artifacts. `cookiespike.sh` and `enforcespike.sh` each own only their exact temporary child cgroup, root-owned privileged snapshot, fixed evidence directory, and verified artifact stage. `dgxstackspike.sh` owns only its run's verified artifact stage, partial work directory, and generic execution-evidence directory and performs no source mutation. `correlationspike.sh` owns only its verified artifact stage, generic execution-evidence directory, and process-lifetime loopback sockets. `tracespike.sh` owns only its verified artifact stage and generic execution-evidence directory. `attackerexecutorspike.sh` owns only its verified artifact stage, generic execution-evidence directory, and one process-lifetime loopback listener. `attackerloopspike.sh` owns those same run-scoped files and one fixture listener plus a mode-0600 marker created immediately before the transient load of the single pinned local model; only that marker authorizes exact-model unload, and its exact cleanup mode cannot name another model. Deployment and explicitly authorized maintenance workflows must define their own similarly narrow ownership and cleanup.

M1C proof evidence is `development-validation-evidence` with `internal-operational` sensitivity. It contains only the run ID, artifact checksum, loopback tuple/cookie proof markers, bounded stdout/stderr, timestamps, fixed safety results, and hashes of the root attachment inventory; it contains no customer traffic, credential, request body, canary value, kubeconfig, or Secret data. Each file is capped at 1 MiB and the only intended retention is the short run→inspect→record→cleanup interval on the DGX. Legal hold is unsupported, deletion is the exact idempotent `--cleanup` path, residency is the DGX filesystem, encryption inherits the operator-managed host boundary, model use is prohibited, lineage is the source-tree/artifact checksum plus run ID, and expected storage is well below 3 MiB per run. Durable non-secret conclusions belong in `docs/DEVELOPMENT_PLAN.md`, not in retained raw proof files.

M1D proof evidence has the same `development-validation-evidence` class and `internal-operational` sensitivity. It contains only the run ID, artifact checksum, explicit synthetic proof-trigger marker, target/control socket-cookie proof markers, bounded stdout/stderr, drop counters, cleanup results, and before/after inventory hashes; it contains no customer traffic, credentials, request bodies, real canary values, kubeconfig, or Secret data. Every file is capped at 1 MiB and expected total storage is below 3 MiB per run. The intended retention is the run→inspect→record→cleanup interval with a 24-hour maximum: before any evidence directory is created, a retrying transient systemd timer schedules exact deletion after 23 hours 55 minutes; completed results record creation, completion, and expiration timestamps derived from one captured completion instant; and the evidence root is `/run/user/1000/canarysting`, so a host reboot deletes it earlier with the runtime tmpfs. Inspection requires the timer to remain active and refuses expired evidence. Legal hold is unsupported; scheduled expiry or descriptor-anchored, inode-verified, quarantine-based, idempotent `--cleanup` performs deletion while retaining descriptors for both the evidence root and the exact run directory through unlink verification. Manual cleanup leaves the retrying expiry path intact until evidence deletion succeeds, then fails unless timer state is queried successfully and both transient units are unloaded. An expiry retry treats an already-absent shared root as successful idempotent completion. Residency is the DGX runtime filesystem; encryption inherits the operator-managed host boundary; model use is prohibited; and lineage is the source-tree/artifact checksum plus run ID and synthetic proof. This synthetic proof evidence must not enter production baselines or customer models. Durable non-secret conclusions belong in `docs/DEVELOPMENT_PLAN.md`.

M2B.2 DGX-stack proof output is also `development-validation-evidence` with `internal-operational` sensitivity. Its published files are seven normalized synthetic observation tombstones, the bounded proof summary, and a result record; each file is capped at 1 MiB and expected total storage is below 3 MiB. Raw source captures are temporary execution inputs only, remain on the DGX, are individually capped, and are deleted before publication. The normalized records use the explicit 24-hour `APPROVED_OVERRIDE` lifecycle, but the intended retention is only run→inspect→record→immediate exact cleanup. Legal hold is unsupported; deletion is `dgxstackspike.sh --cleanup` through the generic run-scoped cleanup. Residency is the DGX filesystem, encryption inherits the operator-managed host boundary, model use is prohibited, and lineage is the source-tree/artifact checksum, run/scenario ID, opaque raw-reference digest, and raw-content checksum. These records are explicitly synthetic and cannot enter the production collector/store, production baselines, customer models, or customer statistics. Durable non-secret conclusions belong in `docs/DEVELOPMENT_PLAN.md`.

M2B.3 correlation-proof output uses the same `development-validation-evidence` class and `internal-operational` sensitivity. Its three files are bounded to 1 MiB each and contain only eight fixed non-secret assertions, empty stderr, and result/checksum/timestamp metadata; expected total storage is well below 3 MiB. The intended lifecycle is run→inspect→record→immediate exact cleanup with a recorded 24-hour maximum. Legal hold is unsupported; deletion is `correlationspike.sh --cleanup` through the generic run-scoped cleanup. Residency and encryption inherit the operator-managed DGX filesystem boundary, model use is prohibited, and lineage is the exact source-tree/artifact checksum plus run/scenario ID. The process-lifetime socket tuples, numeric cookie, and pseudonymization key are not evidence and are never written. The proof is synthetic validation only and cannot enter production observations, baselines, customer models, or customer statistics. Durable non-secret conclusions belong in `docs/DEVELOPMENT_PLAN.md`.

M2B.4/M2B.5 trace-proof output uses the same `development-validation-evidence` class and `internal-operational` sensitivity. Its three files are bounded to 1 MiB each and contain only nine fixed non-secret assertions, empty stderr, and a fixed 19-line result schema with checksums, UTC timestamps, and exact source/artifact/run/scenario lineage; expected total storage is well below 3 MiB. The scenario field must equal `m2b5-operator-conflict`. The validator requires a successful artifact exit, exact stdout, empty stderr, a run duration of at most 30 seconds, and expiry exactly 24 hours after completion and still in the future. The intended lifecycle is run→inspect→record→immediate exact cleanup with that enforced 24-hour maximum. Legal hold is unsupported; deletion is `tracespike.sh --cleanup` through the generic run-scoped cleanup. Residency and encryption inherit the operator-managed DGX filesystem boundary, model use is prohibited, and lineage is the exact source-tree/artifact checksum plus run/scenario ID. In-process synthetic records, opaque references, trace IDs, and hashes are assertions' transient inputs and are never emitted. This validation evidence cannot enter production observations, traces, baselines, customer models, or customer statistics. Durable non-secret conclusions belong in `docs/DEVELOPMENT_PLAN.md`.

M2C.3 bounded-executor proof output uses the same `development-validation-evidence` class and `internal-operational` sensitivity. Its three files are each capped at 1 MiB and contain only eight fixed non-secret assertions, empty stderr, and an exact 20-line result schema with checksums, UTC timestamps, and source/artifact/run/scenario lineage; expected storage is well below 3 MiB. The scenario is fixed to `m2c3-bounded-executor`. Validation requires an unprivileged successful exit, exact stdout, empty stderr, no model execution or raw identifier emission, at most 30 seconds of runtime, and expiry exactly 24 hours after completion and still in the future. The lifecycle is run→inspect→record→immediate exact cleanup; legal hold is unsupported and deletion is `attackerexecutorspike.sh --cleanup` through generic exact-run cleanup. Residency and encryption inherit the operator-managed DGX filesystem boundary, model use is prohibited, and derivation lineage is the exact source tree, artifact checksum, run, and scenario. The transient listener, raw address/port, fixture payload/credential, requests, headers, and response content are never evidence. This proof cannot enter production observations, baselines, customer models, or customer statistics. Durable non-secret conclusions belong in `docs/DEVELOPMENT_PLAN.md`.

M2C.4 bounded-planner proof output uses the same `development-validation-evidence` class and `internal-operational` sensitivity. Its three final files are each capped at 1 MiB and contain only eight fixed non-secret assertions, empty stderr, and an exact 24-line result schema with UTC timestamps, source/artifact/run/scenario lineage, the reviewed planner version, the public local model name/ID, output-minimization markers, and model-cleanup status; expected final storage is well below 3 MiB. A 32-byte mode-0600 ownership marker exists only from immediately before possible model load through the independent zero-loaded postcheck and is never published evidence. Raw prompts, model prose/reasoning, tool-call IDs/names/arguments, target/address/port, request/response content, and credentials are transient inputs and never evidence; only a SHA-256 of the bounded model response enters the ephemeral ground-truth intent. The lifecycle is run→inspect→record→immediate exact cleanup with a recorded 24-hour maximum. Legal hold is unsupported; deletion is scenario-specific attacker-loop cleanup followed by generic exact-run cleanup. Generic read-only inspection validates and reports an exact safe marker for recovery, while generic mutation refuses every active marker. Residency and encryption inherit the DGX filesystem boundary, and use of this evidence in any model or production/customer dataset is prohibited. The fixture socket and Qwen load are process/run lifetime only; the fixed prompt-free cleanup operation retains retry authority until the post-run inspector proves zero loaded models, while an absent marker forbids unload. Durable non-secret conclusions belong in this document and `docs/DEVELOPMENT_PLAN.md`.

Collected M1B bundles are `development-validation-evidence` with `internal-operational` sensitivity. They use an explicit 72-hour ephemeral retention profile and carry collection/expiration timestamps, payload size, source mode, checksum lineage, residency, encryption-boundary, and model-use metadata. The collector does not schedule hidden deletion: the operator removes the exact local output directory at or before expiration after durable non-secret conclusions have been recorded in the development plan. Legal hold is intentionally unsupported for this temporary bundle; evidence needing a hold must be exported through a future governed case store. Local filesystem encryption and residency are operator-managed, model use is prohibited, and the bundle must never feed production baselines or customer models. Refusal—not partial masking—is the boundary for credentials, canary secret values, authorization headers, kubeconfig key/certificate data, request bodies, and other prohibited payloads.

## Validation tiers

### Tier A: local

Use for documentation, pure Go logic, serialization, generated code, static analysis, and non-kernel refactors. Typical gates are formatting, vet, focused and race tests, build, generated drift checks, and frontend lint/build when relevant.

### Tier B: local plus DGX integration

Required for `bpf/`, socket cookies, cgroups, kernel enforcement, Cilium interaction, ARM64 behavior, Envoy-to-kernel attribution, DGX scripts, and low-level networking. Tier A must pass, followed by the relevant deterministic DGX proof and cleanup verification.

### Tier C: local plus DGX Kubernetes end-to-end

Required for identity, Kubernetes ingestion, the operator, CRDs/manifests, DeceptionPolicy reconciliation, workload scope mapping, policy ingestion, Kubernetes-derived graphs, complete request-to-verdict behavior, and Kubernetes enforcement. Tier A and relevant Tier B primitives must pass before the DGX K3s end-to-end test.

### Tier D: DGX attacker/correlation

Required for CanaryAttacker scenarios, Ollama/Qwen execution, ground-truth emission, and correlation-quality claims. Tier A and any relevant Tier B/C primitives must pass first. A Tier D run gathers before-state, emits intent, executes only bounded allowlisted tools, gathers independent observations, correlates them against ground truth, calculates declared metrics, cleans run-owned state, and gathers after-state.

## Risk-tiered gate frequency

Validation tier describes what environment a change ultimately requires; PR risk describes when and how much of that tier runs. Use `make check-fast` while editing, `make check-pr-local` before push, and the authoritative CI `make check-pr` profile for merge. LOW/STANDARD changes do not use DGX. HIGH changes use remote smoke only when their path/dependency map requires it. CRITICAL kernel, containment, attacker-authority, or DGX-harness changes require the relevant remote profile. Unknown impact expands to HIGH, and a manual override can only increase coverage.

The complete Level 3 local and DGX matrices run once per integration/main batch, nightly, on demand, and before release; they are not required after each repair. Weekly Level 4 adds soak and live campaigns. The M2C.4 bounded Qwen loopback smoke is implemented, but the scheduled campaign still fails closed pending M2C.5 real Kubernetes fixtures and the remaining M2D ground-truth/campaign laboratory rather than granting the model any substitute authority. See `docs/CI_TESTING_STRATEGY.md`.

For compatible workflows, `scripts/dgx/pr.sh` performs one read-only preflight, one allowlisted ARM64 build, and one checksum-verified transfer. Its short-lived proof is bound to host, run ID, source revision, and age. Leaf scenarios may reuse only that read-only inspection; they still verify artifacts and before/after state, enforce exact scope, and clean run-owned state. The `attacker-check` profile is passive inspection only: it exits before the shared preflight and runs `scripts/dgx/attackercheck.sh --summary`, with no BPF feature probe, artifact build, transfer, remote write, model execution, or cleanup obligation. The `attacker-executor` profile builds and transfers only its fixed proof artifact, executes no model, and uses the generic exact-run stage/evidence cleanup. Superseded PR jobs are cancelled by CI, and the DGX PR job has a 20-minute overall timeout. Do not duplicate a full manual DGX run in CI for the same commit.

Risk classification retains every required remote profile. The PR DGX job maps and runs each implemented profile under a distinct bounded run ID; it does not let an earlier kernel, enforcement, trace, or harness match hide another selected qualification, and it fails closed if any selected profile is unavailable.

### DGX CI controller

GitHub Actions DGX jobs require one repository-level self-hosted runner on the Mac with the custom label `canarysting-dgx-controller`. The runner is a user-level macOS LaunchAgent installed outside the repository; it is not installed on the DGX and does not change DGX configuration. GitHub-hosted runners cannot use the workstation's private `falcon1` SSH boundary, so the controller is required only for risk-selected DGX, Level 3, and Level 4 CI profiles. If it is absent or offline, those jobs remain queued with no runner assignment and no DGX mutation.

The controller runs repository workflows with the workstation user's authority and may reach only the bounded repository DGX entry points used by the selected job. Its registration credential remains in the runner installation outside the repository; one-time registration tokens, PATs, SSH credentials, and runner diagnostics must never enter source, CI output, DGX artifacts, or test-gate results. The workstation owner operates and upgrades the runner. Failure stops remote qualification; it must never cause a hosted fallback, weaker profile, or manual passing status. Removal requires stopping and uninstalling the LaunchAgent and removing the repository runner registration before deleting its dedicated installation directory.

`actions/setup-go` caching is disabled in self-hosted DGX jobs. The persistent controller already retains its local Go caches, while action caching would archive and upload the workstation user's global multi-project cache. Hosted jobs keep their language-native caches. The runner adds no customer deployment footprint and applies to development infrastructure across all four product deployment profiles.

### Read-only CanaryAttacker readiness inspection

`scripts/dgx/attackercheck.sh` is the fixed M2C.1 readiness inspector for `falcon1`; it accepts only `--dry-run` and `--summary`, requires hostname `spark-5343` and architecture `aarch64`, and expects `qwen3-coder:30b-a3b-q8_0`. It collects a strict ordered version-2 report for host resources and synchronized time, GPU identity/probe state, Ollama CLI/service/API/listener/model state, K3s node readiness, Cilium component readiness, and Hubble Relay readiness. The target lab and CanarySting runtime remain explicitly `unconfigured` until M2C.5 defines exact namespace/workload/service endpoints; cluster-wide names or labels are not scanned and never establish target readiness. Ollama commands are pinned to `http://127.0.0.1:11434` with inherited proxy variables removed, and the version endpoint is fixed HTTP loopback with proxy bypass. Every sensitive remote operation passes a runtime exact-argument allowlist: service inspection is limited to the named Ollama/K3s properties, Kubernetes access to the named node and four named workload readiness fields, Ollama access to version/list/ps, curl to the fixed version URL, GPU/time inspection to fixed queries, and socket inventory to one positive PID. Listener discovery obtains the Ollama systemd service's exact `MainPID` and `InvocationID`, performs one privileged read-only inventory of all TCP listeners, and retains only sockets whose process metadata has that exact PID; consequently a non-loopback Ollama listener on any port fails rather than escaping a fixed-port scan. The same exact service identity and complete owned-listener set are sampled again after the fixed API and model-inventory queries. Any turnover or listener-set change invalidates all API/model evidence and fails closed. Every remote command runs under a command-specific bound, and the entire remote shell is bounded. The captured remote program is pinned by an exact reviewed SHA-256 in addition to the readable sensitive-call manifest and mutation checks, so any executable change—including an unlisted executor—requires an explicit review update without running the captured program locally. It never sends a prompt, loads or runs a model, pulls or changes a model, changes a service/listener, reads Secret or kubeconfig contents, invokes a BPF capability probe, or mutates Kubernetes, Cilium, BPF, firewall, filesystem, or host state. Listener ownership, state-change, or inventory errors, Ollama API/inventory errors, and GPU inventory errors are explicit and make the command fail; unavailable Kubernetes readiness remains explicit and blocks readiness. The local validator recomputes telemetry, safety, invalidation relationships, and the complete ordered blocker set from the underlying facts. The report always sets `live_scenario_ready=false`; missing execution prerequisites remain named blockers for later M2C tasks rather than being repaired or silently accepted.

The initial 2026-09-06 version-1 read-only inspection found Ollama CLI/API `0.32.0` active and enabled as user `ollama`, listening only on `127.0.0.1:11434`. The exact 32 GB `qwen3-coder:30b-a3b-q8_0` model was present with ID `7b438a19895a`; `ollama ps` reported zero loaded models. The host was `spark-5343`, Ubuntu 24.04 on `aarch64`, kernel `6.17.0-1032-nvidia`, with 20 CPUs, 127,600,752 KiB memory, about 3.5 TiB free on `/`, and one NVIDIA GB10 using driver `580.173.02`; `nvidia-smi` did not expose discrete GPU-memory counters on this unified-memory platform. NTP synchronization was true and the timezone was `America/Chicago`. K3s and the node were ready, and the Cilium, operator, and Envoy readiness pairs were each `1/1`; Hubble Relay was absent. Version 2 retains those environmental observations but deliberately withdraws version 1's heuristic target-lab conclusion: M2C.1 reports the target as `unconfigured`, and later work must verify exact lab objects before execution. Live execution remains blocked by the unconfigured target, unready Hubble Relay, absent or unverified CanarySting runtime, and bounded attacker harness.

The repaired version-2 direct and coordinator inspections at `2026-09-06T17:51Z` both passed the strict report validator. They confirmed successful listener, fixed-loopback API, and model-inventory probes; loopback-only binding; the expected model present; zero loaded models; and core telemetry ready with partial sources. The coordinator reported `preflight_count=0`, one passive check, and zero builds/transfers. Its fixed M2C.1 report retains the decomposition blocker name `bounded_attacker_harness_unimplemented`; M2C.4 now supplies that bounded loop for a process-local fixture, while the unconfigured Kubernetes target/runtime and unready Hubble Relay remain M2C.5 work.

The post-review M2C.4 qualification run `m2c4-g3-20260907-06` passed on 2026-09-07. It checksum-built and transferred one Linux/ARM64 artifact from source-tree digest `decfcd4edb286e3a342236bcc267c230d3e63659da99ded3788436f3ebf82fd3`; verified the exact model/binding and zero loaded models before the run; accepted one Qwen tool call using the bounded live response shape; executed one exact reviewed HTTP action against the process-lifetime loopback fixture; committed one intent and one action; stopped with `scenario_complete`; published and re-inspected its strict evidence; verified the model unloaded and DGX/K3s/Cilium/CanarySting/BPF health unchanged; then exercised the recovery path, which ran the fixed prompt-free unload and verified zero loaded models before removing the evidence and stage. The coordinator's subsequent generic cleanup and a separate repeated leaf cleanup both found the run state absent. No real Kubernetes target or CanarySting runtime was claimed by this proof.

The inspection output is `development-validation-evidence` with `internal-operational` sensitivity. It contains only normalized facts and named blockers; it contains no prompts, responses, model contents, customer data, credentials, kubeconfig, Secret data, raw traffic, or source-owned evidence. The full mode-0600 temporary report is deleted locally on every exit and no remote artifact is created, so direct runs retain no file after the command. CI requests the minimized summary only; that summary resides in GitHub Actions logs under the repository's configured Actions retention and GitHub-managed encryption/residency boundary, is deleted by run/log deletion or policy expiry, and is invalid when its run is deleted. The harness has no legal-hold feature; a required hold must use an approved evidence process. Model use is prohibited. Expected CI storage is less than 2 KiB per run. Durable non-secret conclusions belong in this document and `docs/DEVELOPMENT_PLAN.md`.

## CanaryAttacker safety boundary

The DGX model may plan only through a reviewed structured tool catalog such as bounded HTTP requests, DNS lookups, TCP connects, endpoint enumeration, same-target link following, explicit lab-fixture credential attempts, and response inspection. The model does not receive arbitrary shell, SSH, Kubernetes, Docker, filesystem, or unrestricted network access. The executor—not the model—enforces target allowlists, method/payload rules, redirects, timeouts, concurrency, request/body/token budgets, credential references, and cancellation.

Every scenario is confined to a dedicated CanarySting lab fixture and emits `AttackerIntent` before an action and `AttackerAction` after it. These records are declared harness ground truth, never trusted network telemetry. Independent Cilium/Hubble, Envoy, kernel, Kubernetes, and CanarySting observations determine correlation quality. Versioned synthetic ground truth may be retained indefinitely as a development-lab evaluation corpus, but it must be marked synthetic and remain outside production baselines, customer behavior models, and production incident statistics. Scenarios must stop processes, remove only run-labeled execution state, verify connectivity/Cilium/BPF after cleanup, and never retain secret values in evidence. See `docs/CANARYATTACKER_ARCHITECTURE.md` and `docs/CANARYVIEW_STORAGE_AND_RETENTION.md`.

## Operating and cleanup rules

Before any DGX mutation, run `scripts/dgx/check.sh`, inspect local Git status, state the exact remote changes and cleanup, and verify that they belong to the selected task. Do not install host packages or alter K3s, Cilium, firewall, SSH, kernel configuration, or system services implicitly.

For eBPF tests, inventory attachments first, use the smallest cgroup/hook scope and a multi-attach-safe mechanism where supported, never replace Cilium programs, collect before/after state, detach CanarySting-owned programs, delete only CanarySting-owned maps/links, and verify unrelated connectivity after cleanup.

For Kubernetes tests, prefer a dedicated namespace, apply declarative manifests, label every resource with the project and unique run identifier, never read or print Secret data, and remove only those labeled resources. Cleanup must be idempotent and must prove the cluster, Cilium, connectivity, BPF attachments, processes, and staging state returned to the recorded baseline.

## Security baseline and hardening backlog

The following lab risks were observed previously and remain backlog items unless a later read-only check proves otherwise:

1. **Highest priority: `/etc/rancher/k3s/k3s.yaml` is mode `0644` and contains cluster-admin credentials.** Correcting its permissions requires explicit approval and post-change K3s/Cilium validation.
2. The current user has passwordless sudo and cluster-admin access; reduce routine workflows to a least-privilege ServiceAccount and narrowly scoped sudo/capabilities.
3. UFW is inactive.
4. SSH listens beyond loopback.
5. The Kubernetes API listens beyond loopback.
6. Cilium health/metrics endpoints, including observed ports 4240 and 9964, listen beyond loopback.
7. Cilium host firewall was previously observed disabled or not configured.
8. Cilium network encryption was previously observed disabled or not configured.
9. No workload NetworkPolicies or CiliumNetworkPolicies existed at the 2026-08-24 inspection.

Do not batch-fix these during ordinary development. Each correction needs an explicit task, approval, rollback, and before/after health evidence. Never commit kubeconfig contents, private keys, bearer tokens, Secret values, or copied credentials.
