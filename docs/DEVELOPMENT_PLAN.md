# CanaryPlatform Development Plan

This is the source of truth for CanaryPlatform development sequence, current work, dependencies, validation, blockers, and completion evidence. M1A through M1D retain their historical CanarySting IDs. Allowed status values are `TODO`, `IN_PROGRESS`, `BLOCKED`, and `DONE`. Normally only one task is `IN_PROGRESS`. A task is `DONE` only after every acceptance criterion and its declared validation tier pass; partial code is not completion.

Validation tiers:

- **Tier A — Local:** documentation, pure Go, static analysis, generated code, serialization, and non-kernel refactoring. Typical gates are formatting, vet, focused/race tests, build, generated drift checks, and frontend lint/build when relevant.
- **Tier B — Local plus DGX integration:** eBPF, socket cookies, cgroups, kernel enforcement, Cilium, ARM64, Envoy-to-kernel attribution, DGX scripts, and low-level networking. Tier A plus the relevant DGX proof and cleanup are required.
- **Tier C — Local plus DGX Kubernetes end-to-end:** identity, Kubernetes ingestion/operator/manifests/CRDs, reconciliation, workload scope, policy ingestion, Kubernetes graph construction, and complete request-to-verdict/enforcement behavior. Tier A, relevant Tier B primitives, and DGX K3s end-to-end validation are required.
- **Tier D — DGX attacker/correlation:** CanaryAttacker scenarios, Ollama/Qwen, emitted ground truth, cross-source correlation, and correlation-quality claims. Tier A and relevant Tier B/C primitives plus before/scenario/observe/correlate/cleanup/after evidence are required.

The skill records one product area (`CanaryView Core`, `CanaryView Agentic Operations`, `Canary Site Gateway`, `CanarySting Managed Assets`, `CanarySting Local Response`, `CanaryAttacker`, `Shared platform`, or `Development infrastructure`), every affected deployment profile, and one equivalent validation label (`local`, `DGX integration`, `DGX Kubernetes end-to-end`, or `DGX attacker/correlation`) before implementation. Historical product-area wording and Tier A/B/C labels remain valid completion evidence and are not rewritten retroactively.

## Milestone summary

| Milestone | Status | Meaning |
|---|---|---|
| S0, M1A | DONE | Codex-native bootstrap and clean local baseline preserved. |
| M1B.1–M1B.4 | DONE | Completed harness progress and implementation evidence preserved. |
| M1B.5 | DONE | The missing redacted collector was recovered with local negative tests and a clean DGX Tier B proof. |
| M1B.6 | DONE | Exact-run, idempotent DGX cleanup workflow implemented and validated. |
| M1B.7 | DONE | Existing development-environment documentation completion preserved and evolved here. |
| M1C | DONE | The observe-only socket-cookie join is repeatably proven on the DGX with root-owned verified execution, live attach-scope evidence, and exact cleanup. |
| M1D | DONE | Precise cookie-scoped enforcement is repeatably proven on the DGX without altering Cilium or bystanders. |
| DI.1 | DONE | Dependency-aware collect-all local gates now retain complete diagnostics, targeted replay, bounded adversarial scenarios, and final full merge proof. |
| M2A.0 | DONE | Storage, retention, deletion, residency, model-use, and logical storage architecture approved before canonical-model implementation. |
| M2A–M2G | TODO | View-first storage/model, read-only Wave 0 connectors, correlation/first trace, CanaryAttacker ground truth, agentic/operator insight, graph, and low-footprint placement intelligence; M2A depends on M2A.0. |
| M3 | TODO | Optional CanarySting deployment profiles, progressing from external/identity assets through Site Gateway, proxy/route, Kubernetes, and separately authorized local response. |
| M4–M7 | TODO | Operator-experience expansion, structured agent interface, category-based connector expansion, and SecurityIntent-driven native action coordination. |
| M8 | TODO | Carried-forward production hardening backlog. |

Each row below contains all mandatory task fields. `Pending` completion evidence means the task cannot be marked `DONE`.

## Bootstrap record

| Task | Status | Objective | Why it matters | Dependencies | Required validation tier | Acceptance criteria | Relevant architecture constraints | Likely files | Validation commands | Implementation notes | Completion evidence | Follow-up work discovered |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| S0 Codex-native bootstrap | DONE | Establish the Mac/DGX model, plan, repo skill, and safe harness scaffolding without runtime changes. | Durable task selection and evidence prevent ad hoc privileged work and false completion. | None | Tier A plus read-only DGX inspection | Requested docs, skill, and scripts exist; skill discovery succeeds; DGX facts are checked read-only; no install, runtime change, commit, push, or unapproved DGX mutation. | All AGENTS rules; Mac canonical; DGX integration-only; no secrets. | `AGENTS.md`, `docs/DEVELOPMENT_*`, `.agents/skills/canarysting-dev/`, `scripts/dgx/` | `git diff --check`<br>`bash -n scripts/dgx/*.sh`<br>skill validator/discovery<br>`scripts/dgx/check.sh`<br>`git status --short` | Non-check scripts fail closed until their own task. | 2026-08-24: skill quick-validation and forced Codex discovery passed; all scripts parsed; read-only DGX check passed; docs/status/tree/diff checks passed; no dependencies, runtime behavior, cluster/BPF state, or security settings changed. | Canceled CI work remains partial; prior interrupted DGX work left a temp directory and Ubuntu image requiring separately approved cleanup. |

## Development infrastructure: validation orchestration

| Task | Status | Product area | Objective | Why it matters | Dependencies | Required validation tier | Acceptance criteria | Relevant architecture constraints | Likely files | Validation commands | Implementation notes | Completion evidence | Follow-up work discovered |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| DI.1 Collect-all local validation gates | DONE | Development infrastructure | Produce a complete independent-failure inventory in one diagnostic run, support compatible targeted repair replays, and retain a final full merge qualification. | The previous serial Make chain discovered one failure per expensive full run and preserved no replay ledger. | S0, M1D.1 | local | Explicit DAG; ordinary collect-all and dependency-aware blocks; safety stops and cleanup; per-run JSON/JUnit/log/repro artifacts; conservative affected selection; adversarial manifest; last-failed/one/repeat commands; deterministic gate self-tests; local and CI share check IDs; full local merge gate passes or reports every blocker. | No runtime behavior change; no weakened or removed assertion; no arbitrary shell/model authority; privileged and DGX work remain separate and fail closed. | `cmd/testgate/`, `internal/testgate/`, `test/gates/`, `Makefile`, `.github/workflows/ci.yml`, `.gitignore`, `docs/TESTING_GATES.md`, this plan, repository skill | focused runner tests; synthetic collect-all/safety/replay proofs; `make check-merge-local`; `make check-merge VALIDATION_TIER=local`; `git diff --check` | 2026-08-27/28: baseline clean-tree warm-cache `make check` passed in about 35.4s. Deterministic self-tests proved two independent failures plus one dependency block while unrelated work continued, process-group timeout termination, cleanup/safety-stop behavior, non-clearing diagnostic retry, compatibility refusal/expansion, and JSON/JUnit/console agreement. A sandbox-denied loopback scenario produced a complete failure ledger at `.test-artifacts/gates/20260828T005808.349362000Z`; exact replay passed in 1.99s and `make check-last-failed` reran only the failed scenario plus four prerequisites in 0.47s. Full cold-cache local qualification passed 33 nodes with 0 failures/blocks/safety stops and two honest Darwin skips in 364.11s at `.test-artifacts/gates/20260828T010003.305040000Z`; 274.72s was cold race compilation, so no raw wall-clock speedup is claimed. No DGX access/mutation, runtime behavior change, dependency addition, commit, or push occurred. Post-review hardening added exact scenario-command validation, parsed assertion evidence, full-stream credential detection, non-cancelled signal cleanup, merge-base affected selection, subset-ledger preservation, and shared privileged-CI orchestration; the expanded `make check-fast JOBS=4` passed 30 nodes with two honest Darwin skips in 45.49s at `.test-artifacts/gates/20260828T030355.401887000Z`; the final full local merge proof passed 33 nodes with two honest Darwin skips, 0 failures/blocks/safety stops, in 69.07s at `.test-artifacts/gates/20260828T173713.066938000Z`. | Privileged eBPF CI now uses the shared `ebpf-privileged` check ID, structured no-skip/PASS-floor parser, and artifact model inside its isolated root Linux container; future task-specific DGX gates must add explicit manifest nodes rather than treating generic preflight as qualification. |
| DI.2 Risk-tiered CI and adversarial validation | DONE | Development infrastructure | Make most pull requests authoritative in under 15 minutes and high-risk pull requests in under 30 minutes by selecting the smallest conservative gate for changed risk while moving complete matrices to integration, nightly, release, and campaign profiles. | Running the same complete local and remote matrix for every PR wastes developer and DGX capacity and delays independent failure feedback. | DI.1 | local | Explainable LOW/STANDARD/HIGH/CRITICAL classification; Level 0–4 commands; fast invariants on executable PRs; affected deterministic replay; conditional privileged/DGX smoke; superseded-run cancellation; complete Level 3/4 profiles; timing budgets and self-tests; shared local/CI check IDs; no removed or weakened test. | Unknown impact expands to HIGH; manual override can only increase risk; CRITICAL work fails closed without relevant remote evidence; no live-model shell/network authority; DGX qualification is limited to an explicitly approved run-owned profile with exact cleanup and no configuration change. | `internal/testgate/`, `cmd/testgate/`, gate/scenario manifests, Makefile, workflows, AGENTS, skill, testing/environment/attacker/strategy docs, this plan | focused risk/selection tests; gate self-tests; `make check-fast`; `make check-pr-local`; risk-profile dry runs; exact-tree broad local validation; `git diff --check` | Product area: Development infrastructure. Validation tier: local plus targeted DGX integration evidence. Implemented explainable risk/path selection, Level 0–4 interfaces, budget reporting, fast named security invariants, conservative Go reverse-dependents, scenario affected paths/forms, non-duplicated manifest-owned replay, conditional CI prerequisites, latest-SHA/cancellation, authoritative aggregation, and a one-preflight/build/transfer DGX coordinator. No product runtime behavior or dependency changed. | 2026-08-28: old exact warm full gate passed in 68.65s at `.test-artifacts/gates/20260828T180353.250803000Z`; recent old CI's longest duplicated job was 12m35s. Risk/selection/unit tests, manifest validation (43 checks/3 scenarios), workflow YAML, shell syntax, all DGX harness contracts, and diff checks passed. A synthetic selected gate collected two independent failures in 30.23s; incompatible replay refused safely and direct replays passed in 0.28s/2.57s. Final Level 0 passed 20/20 in 22.62s at `.test-artifacts/gates/20260828T185941.425479000Z`. Level 1 passed 29/29 in 325.40s at `.test-artifacts/gates/20260828T184920.748998000Z`; its cold changed race command took 190.33s. Level 3 then reused that cache and passed 34 checks with two honest Darwin eBPF skips in 103.64s at `.test-artifacts/gates/20260828T185454.886105000Z`. No DGX access/mutation, commit, or push occurred. 2026-08-31: the approved exact-tree `kernel-full` profile passed for dirty tree digest `7daac0c6afd050cc3cf5bf07550d368ab84d060e2dc0d3125b1273d796533ac6` at revision `7b26c3eb1d864b04530ba943b85eaea4af9f664a` under run `di2-20260831-01`. One preflight/build/transfer served both socket-cookie and enforcement scenarios; both passed. Root attachment and BPF inventory hashes were unchanged, K3s/Cilium/Envoy stayed healthy, and exact cleanup removed the stage, evidence, child cgroup, snapshots, processes, and BPF residue. No DGX configuration changed. PR #97's authoritative HIGH Level 2 workflow [run 33455087048](https://github.com/henleda/canarysting/actions/runs/33455087048) then passed for exact head `07f12947d14d5d8f05b740803059b5b8c185d55b`: risk classification succeeded, Level 2 local completed in 12m32s, the authoritative aggregator succeeded, and the full workflow completed in 13m52s, within the 30-minute HIGH budget. Risk-unselected DGX, privileged eBPF, Level 3, and Level 4 jobs skipped as designed. | Kubernetes smoke remains unimplemented, and scheduled Level 4 live Qwen fails closed until M2C supplies the bounded runtime/tool contract. Cold race compilation remains the next optimization target. These are dependency-scoped follow-ups rather than DI.2 completion blockers. |

| DI.3 Source-tree build isolation | DONE | Development infrastructure | Prevent deterministic source-fingerprint checks from overlapping checks that mutate build outputs in the shared checkout. | Hosted Level 3 repeatedly failed when `dgx-harness:build` fingerprinted transient eBPF compiler state while `bpf-compile` ran concurrently. | DI.2 | local plus on-demand Level 3/DGX integration | DGX build-harness and eBPF compile/assert nodes share one explicit workspace isolation boundary; deterministic self-tests prove same-key checks never overlap and the repository manifest retains that policy; all checks/assertions remain required; focused tests, local PR gate, hosted Level 3, and its bounded DGX matrix pass. | No product/runtime behavior change; do not weaken deterministic manifests or eBPF coverage; use existing bounded DGX preflight/build/transfer/cleanup only. | `test/gates/checks.json`, `internal/testgate/*_test.go`, `docs/TESTING_GATES.md`, this plan | focused scheduler/manifest tests; `make check-fast`; `make check-pr-local`; pushed PR Level 2; on-demand Level 3 integration workflow including existing `kernel-full` DGX profile | Product area: Development infrastructure. Deployment profiles: all four because the shared qualification system protects every profile. Validation: local plus explicitly approved on-demand Level 3/DGX integration. PR risk: HIGH for gate-runner/manifest paths; the on-demand integration workflow supplies broader Linux concurrency and DGX evidence. No ad hoc DGX configuration or product state change is authorized. | 2026-09-01: `dgx-harness:build`, `bpf-compile`, and `bpf-object-assert` now share the `workspace-build` isolation key. Scheduler and repository-manifest regression tests passed with `go test -race ./internal/testgate`. Current-tree `check-fast` passed 11/11 in 23.95s at `.test-artifacts/gates/20260901T144214.184574000Z`; `check-pr-local` passed 20/20 in 98.44s at `.test-artifacts/gates/20260901T144318.897615000Z`; full local Level 3 passed 34 checks with two expected Darwin eBPF skips, zero failures/blocks, in 65.54s at `.test-artifacts/gates/20260901T145509.248242000Z`. PR #100 exact head `76eef31a798e8c44b6769f4e00b7fd694b4f2141` passed the automatically created PR [run 33524829779](https://github.com/henleda/canarysting/actions/runs/33524829779) and the manually dispatched authoritative HIGH Level 2 [run 33525546518](https://github.com/henleda/canarysting/actions/runs/33525546518): Level 2 local passed in 12m13s and the aggregator passed. The manual fallback was dispatched because the automatic run was not yet visible; GitHub created the delayed event afterward. The same head passed on-demand Level 3 [run 33524616203](https://github.com/henleda/canarysting/actions/runs/33524616203): hosted Linux qualification passed in 17m40s and the full implemented DGX matrix was assigned immediately and passed in 1m06s. DGX after-state reported healthy K3s/Cilium/Envoy, no named CanarySting processes/programs/maps/links or matching test pods, and `post_cleanup_check=PASS`. The controller is a persistent Mac user service outside the repository; no DGX configuration changed. Disabling action-cache upload only on self-hosted DGX jobs removed the prior approximately 50-minute global-cache upload tail while preserving hosted caches. | No functional blocker. GitHub Actions' `actions/setup-go@v5` emits a Node 20 deprecation warning under the current runner but runs successfully under the forced Node 24 runtime; track the upstream action upgrade separately. |

## Milestone 1A: Clean local development baseline

| Task | Status | Objective | Why it matters | Dependencies | Required validation tier | Acceptance criteria | Relevant architecture constraints | Likely files | Validation commands | Implementation notes | Completion evidence | Follow-up work discovered |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| M1A.1 Update AGENTS status | DONE | Match implementation-status statements to current code. | Codex must distinguish built, prototype, and absent components. | None | Tier A | Status/layout agree with code and gap verification; all eleven rules remain. | Honesty discipline; no intent change. | `AGENTS.md`, `docs/GAP_VERIFICATION.md` | `git diff --check -- AGENTS.md`; path inventory; focused tests | Reconciled stale additive scaffold. | 2026-08-24: operator/identity marked partial; graph/K8s ingestion/deployment planned; checks passed. | CLAUDE.md remains stale and non-authoritative; preserve it during bootstrap. |
| M1A.2 Normalize untracked work | DONE | Review `AGENTS.md` and both spikes without discarding user work. | These files carry architectural and privileged-test intent. | M1A.1 | Tier A review/cross-build | Files are intentional, non-secret, formatted, documented, and cross-build clean. | Preserve user work; spikes are diagnostics, not production claims. | `AGENTS.md`, `cmd/cookiespike/`, `cmd/enforcespike/` | `git status --short`; `gofmt -d`; Linux/ARM64 build and vet | No spike runtime change. | 2026-08-24: review/scan/gofmt/cross-build/vet and production-seam tests passed. | Productize only through M1C/M1D. |
| M1A.3 Restore local gates | DONE | Pass fmt, vet, build, test, self-check, and aggregate gates. | Later work needs a trusted baseline. | M1A.2 | Tier A | Every named gate passes without weakening/skipping. | Never bypass gates or invariants. | `Makefile`, implicated Go/tests | `make fmt-check`; `make vet`; `make build`; `make test`; `make selfcheck`; `make check` | No install. | 2026-08-24: all gates passed on Darwin/ARM64; only non-fatal linker warning. | Privileged Linux evidence remains Tier B. |
| M1A.4 Fix cookiespike formatting | DONE | Correct `cmd/cookiespike/main.go` formatting. | Diagnostics must satisfy local gates. | M1A.2 | Tier A | gofmt clean and ARM64 cross-build succeeds. | Formatting only; no join behavior change. | `cmd/cookiespike/main.go` | `gofmt -d cmd/cookiespike/main.go`; ARM64 build | Comment formatting/rule reference normalized. | 2026-08-24: gofmt, build, and vet passed. | M1C integration proof. |
| M1A.5 Add sockops build coverage | DONE | Compile `bpf/sockops/*.bpf.c` through the normal target. | The load-bearing join source must not drift behind committed objects. | M1A.3 | Tier B | Enforce, observe, and sockops actually compile on Linux ARM64; non-Linux behavior stays explicit. | Cookie is sole join; no BPF behavior change. | `Makefile`, `bpf/{enforce,observe,sockops}/` | `make -n UNAME_S=Linux bpf`; DGX `make bpf`; `git diff --check -- Makefile` | The existing uncommitted Makefile change was sufficient; validation compiled only and did not load or attach BPF programs. | 2026-08-24: Darwin `make bpf` skipped explicitly; forced-Linux dry-run listed enforce, observe, and sockops; `make check` passed. On `spark-5343` ARM64, the normal `make bpf` target built three non-empty eBPF ELF objects in an auto-removed Ubuntu 24.04 container using headers generated from the host BTF. Pre/post `scripts/dgx/check.sh` showed K3s Ready, Cilium/Envoy healthy, identical Cilium-only root attachments, and no CanarySting process, BPF, or Kubernetes state. `/var/tmp/canarysting/m1a5-20260824-01` and its newly created parent were removed and verified absent. | M1B build/copy/cleanup harness remains required to replace this bounded one-time transfer procedure. |
| M1A.6 Compile sockops in CI | DONE | Make CI compile current sockops source via the normal target. | CI must fail on omission or source failure. | M1A.5 | Tier B | CI generates headers, builds/asserts all objects, and preserves privileged no-skip tests. | Do not trust only committed objects or weaken runtime gates. | `.github/workflows/ci.yml`, `Makefile` | YAML parse; workflow diff check; Linux `make bpf`; review | Pinned the privileged test step to Bash after hosted evidence showed its container defaulted to `sh`, which rejected Bash-only `pipefail`; the no-skip/PASS-floor guard remains unchanged. | 2026-08-25: local YAML/shell checks and `make check` passed. On `spark-5343`, the GitHub-equivalent privileged ARM64 container ran 18 tests with 0 skips and 0 failures, including all 6 required proofs. Pre/post checks showed identical Cilium-only root attachments, K3s Ready, Cilium/Envoy healthy, and no residual CanarySting process/BPF/Kubernetes state; stage, container, temporary image, and local artifacts were removed. Corrected hosted push run 32876171695 and PR run 32876176765 passed clang, Go fmt/vet/build/test, and the privileged job; each privileged guard reported 18 pass, 0 skip, 0 fail, with all 6 required proofs green. | Permanent M1B automation remains required. GitHub actions/cache@v4 and setup-go@v5 emit Node 20 deprecation warnings while GitHub forces Node 24; update the actions in a separate CI-maintenance task. |
| M1A.7 Generated drift checks | DONE | Detect protobuf and CRD/deepcopy drift. | Committed APIs must be reproducible. | M1A.3 | Tier A | Version-recorded deterministic generation and clean-worktree drift checks run locally/CI. | Contract changes deliberate; no implicit install. | `Makefile`, CI, `api/gen/`, `config/crd/`, operator API | generated target; before/after status; generated path diff | Added isolated check/write modes with exact version checks for protoc 35.0, protoc-gen-go 1.35.2, protoc-gen-go-grpc 1.5.1, and controller-gen 0.18.0; `make check` and CI now run `generated-check`, and no tool is installed implicitly by the repository script. The controller-gen probe captures only tool stdout so clean-cache Go download notices on stderr cannot corrupt the exact version comparison. | 2026-08-25: `make generated-check` reproduced protobuf, deepcopy, and CRD output byte-for-byte; `make generated` left all committed generated paths unchanged; intentionally stale output and wrong-version fixtures failed with actionable diagnostics. Hosted run 32896450644 exposed the clean Go-cache stderr issue; a simulated download notice subsequently passed while a wrong controller-gen version still failed. Final push run 32899651608 and PR run 32899657341 passed generated-check, fmt, vet, build, and the full race suite. The complete local `make check` also passed; only the known non-fatal macOS linker warning appeared. No runtime/generated artifact changed, and DGX was not accessed. | Extend the operator artifact inventory in `scripts/generated.sh` when a second CRD or API package is introduced. |
| M1A.8 Frontend lint/build gate | DONE | Add existing dashboard lint/build without feature expansion. | Operator UI needs a deterministic quality floor. | M1A.3 | Tier A | Lockfile-based lint/build run locally/CI; no unrelated UI work. | Dashboard display-only/off verdict path. | `dashboard/app/`, `Makefile`, CI | `npm run lint`; `npm run build` | Added `frontend-check` with explicit npm, lockfile, and installed-tree prerequisite checks; it disables telemetry and runs the existing lint/build scripts. CI pins Node.js 24.19.0 with bundled npm 11.17.0, asserts both versions, caches from the dashboard lockfile, installs exactly with `npm ci`, then exercises the same Make target. `packageManager` records the npm pin; the repository never installs dependencies implicitly. | 2026-08-25: hosted runs 32896450644 and 32897781551 exposed lock metadata produced by older npm versions; the lockfile was regenerated with pinned npm 11.17.0 and passed `npm ci --dry-run` from an isolated directory without `node_modules`. Final push run 32899651608 and PR run 32899657341 passed the toolchain assertion, clean `npm ci`, ESLint with zero warnings/errors, and the production build of all 12 routes. The complete local `make check` passed generated drift, frontend lint/build, fmt, vet, build, full race tests, and both self-checks. No dashboard source or declared dependency changed, and DGX was not accessed. | Behavioral frontend tests remain tracked in M5. |

## Milestone 1B: Permanent DGX development harness

| Task | Status | Objective | Why it matters | Dependencies | Required validation tier | Acceptance criteria | Relevant architecture constraints | Likely files | Validation commands | Implementation notes | Completion evidence | Follow-up work discovered |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| M1B.1 Read-only DGX check | DONE | Inspect host, cluster, Cilium, BPF, policy, and CanarySting state concisely. | Privileged work requires trustworthy before-state. | S0 | Tier B read-only | All requested facts inspected; failures explicit; no secrets/state changes. | Inspect before mutate; report unexpected state. | `scripts/dgx/check.sh`, environment doc | `bash -n scripts/dgx/check.sh`; run against `falcon1` | Uses alias and kubeconfig path without reading contents. | 2026-08-24: script syntax and read-only DGX run passed; expected ARM64/K3s/Cilium/eBPF state confirmed and no CanarySting process, named BPF state, Kubernetes resource, test pod, or policy found. | Add machine-readable mode only when needed. |
| M1B.2 Mac ARM64 builds | DONE | Deterministically build named Linux/ARM64 artifacts on Mac. | DGX runs artifacts, not source edits. | M1A.3, M1B.1 | Tier B | Tool checks, explicit outputs, source state/checksums, test/product distinction. | DGX not canonical; no host install. | `scripts/dgx/build.sh`, `scripts/dgx/build_test.sh`, environment doc | `bash -n scripts/dgx/*.sh`<br>`scripts/dgx/build_test.sh`<br>`scripts/dgx/build.sh --output-dir /tmp/... --all`<br>`make check`<br>DGX pre/post check and bounded execution | Added an explicit seven-target catalog, offline local-toolchain build settings, atomic no-overwrite output, product/test directories, ARM64 ELF assertions, and a source-revision/tree manifest plus SHA-256 inventory. `product/` classifies deployable components and does not claim production maturity. | 2026-08-25: focused tests rejected missing/unknown/duplicate targets, missing-parent and in-repository destinations, and overwrite attempts without creating or changing refused paths; verified manifest checksums and ARM64 ELF type; and produced byte-identical `engine`/`cookiespike` outputs across two builds. A full Mac build produced all five product and two diagnostic artifacts with Go 1.25.3, `CGO_ENABLED=0`, `GOOS=linux`, `GOARCH=arm64`, `GOARM64=v8.0`, local toolchain/module cache only, and no install/download. On `spark-5343`, all seven files matched the Mac checksums and their unprivileged help paths executed successfully. The exact `/var/tmp/canarysting/m1b2-20260825-01` stage was removed and its parent verified absent. Pre/post checks showed K3s Ready, Cilium/Envoy healthy, the same 11 Cilium-only root attachments, and no CanarySting process, BPF, Kubernetes, test-pod, or policy state. `make check`, shell syntax, and diff checks passed; only the known non-fatal macOS linker warning appeared. Local test artifacts were removed. | M1B.3 must replace the bounded manual transfer with checksum-verified automation. Add image cross-build when deployable image assets exist. |
| M1B.3 Isolated transfer | DONE | Copy only approved artifacts to a run-specific stage. | Prevent partial checkouts/hidden state. | M1B.2 | Tier B | Verify host/arch; explicit `/var/tmp/canarysting/` run dir; manifest and checksum verification. | Never transfer credentials; no broad copy/delete. | `scripts/dgx/copy.sh`, `scripts/dgx/copy_test.sh`, environment doc | `bash -n scripts/dgx/*.sh`<br>`scripts/dgx/copy_test.sh`<br>`scripts/dgx/copy.sh --artifact-dir /tmp/... --run-id ... --dry-run`<br>`scripts/dgx/copy.sh --artifact-dir /tmp/... --run-id ...`<br>`scripts/dgx/copy.sh --verify-only --run-id ...`<br>`make check`<br>DGX pre/post check and exact cleanup | Added a fixed-`falcon1` transfer with strict run IDs; exact manifest, provenance, target, size, checksum, executable, file-type, directory, and allowlist validation on both hosts; architecture/hostname preflight; mode-0700 incoming stages; atomic publication; read-only re-verification; no-overwrite behavior; and exact partial-stage failure cleanup. The command transfers binaries only and never executes them. | 2026-08-25: shell syntax, focused positive/negative transfer tests, diff checks, and `make check` passed. Negative tests rejected malformed/unsafe IDs, extra/missing/corrupt/symlinked artifacts, traversal paths, and malformed checksum inventories. A dry run validated a two-artifact bundle without contacting the DGX. On `spark-5343`, `product/engine` and `test/cookiespike` were copied to `/var/tmp/canarysting/m1b3-20260825-01`, remotely re-verified against revision `026db8ad3901c4dd181f0ae67981ea9da5604032` and tree digest `44a910628215f652c546f933c6f3d395e2b277f2b417db1e145c1d2365564849`, and a repeated transfer was refused before copy. The exact final stage and local bundle were removed; no incoming stage or parent remained. Pre/post checks showed K3s Ready, Cilium/Envoy healthy, the same 11 Cilium-only root attachments, and no CanarySting process, BPF, Kubernetes, test-pod, or policy state. | M1B.4 should consume only a verified named run stage. M1B.5 must define evidence retention/age policy; M1B.6 owns generalized idempotent cleanup. |
| M1B.4 Deterministic execution | DONE | Run named artifacts with bounded args/time/privilege/run ID. | Replace improvised SSH with repeatable tests. | M1B.3 | Tier B | Validate prerequisites/ownership; record result; timeout; refuse undeclared mutation. | Minimal scope; inspect attachments first. | `scripts/dgx/run.sh`, `scripts/dgx/run_test.sh`, environment doc | `bash -n scripts/dgx/*.sh`<br>`scripts/dgx/run_test.sh`<br>`scripts/dgx/run.sh --run-id ... --profile ... --dry-run`<br>`make check`<br>verified stage plus harmless self-check and timeout/SIGTERM profiles<br>result/mode/process inspection<br>DGX pre/post check and exact cleanup | Added a fixed profile catalog whose remote half independently derives the artifact, arguments, timeout, expected outcome, and unprivileged execution mode. Every run re-verifies the immutable copy stage, stage/artifact ownership, and artifact checksum; uses a clean environment and in-memory/listener-free/BPF-free engine invocation; writes mode-restricted bounded logs and an atomic result to a separate non-overwritable run directory; and scans `/proc` for artifact-process residue. Arbitrary commands, arguments, timeouts, paths, and privilege escalation are not accepted. | 2026-08-25: shell syntax, focused profile/dry-run/input-refusal tests, diff checks, and `make check` passed; the full gate covered generated drift, frontend lint/build, vet, build, race tests, and self-checks, with only the known non-fatal macOS linker warning. A one-artifact ARM64 engine bundle for revision `026db8ad3901c4dd181f0ae67981ea9da5604032` and tree digest `5d4b34d39a85bf85f35e1f2ff472cd429beab76c07d389fd8811859a3a2e339b` was transferred to two verified `spark-5343` stages. `engine-selfcheck` passed unprivileged with exit 0; `engine-timeout-probe` reached ready state, handled SIGTERM, and passed with the fixed timeout exit 124. Both reported `process_residue=none`, wrote only `stdout.log`, `stderr.log`, and `result.tsv` under mode-0700 evidence directories with mode-0600 files, and recorded PASS. A repeated execution was refused before running because its evidence directory existed. The exact two stages, two evidence directories, parent, and local bundle were removed. Pre/post checks showed K3s Ready, Cilium/Envoy healthy, the same 11 Cilium-only root attachments, and no CanarySting process, BPF, Kubernetes, test-pod, or policy state. | M1B.5 should collect only the declared result/log schema with redaction and retention bounds. M1B.6 should generalize exact stage/evidence cleanup. Privileged execution remains owned by the narrowly scoped M1C/M1D harnesses, not this generic runner. |
| M1B.5 Redacted collection | DONE | Collect logs and before/after evidence without secrets. | PASS/FAIL must be diagnosable. | M1B.4 | Tier B | Declared logs/checksums/cluster/Cilium/BPF/connectivity evidence; prohibited data redacted. | No kubeconfig/Secret/raw cross-boundary data. | `scripts/dgx/collect.sh`, `scripts/dgx/collect_test.sh`, external ephemeral evidence directory | `bash -n scripts/dgx/*.sh`<br>`scripts/dgx/collect_test.sh` plus all M1B harness tests<br>`make check`<br>build/copy/run/collect/manifest scan/cleanup twice on `falcon1` | Product area: Development infrastructure. An exhaustive 2026-08-26 search of reachable refs, reflogs, stashes, dangling Git objects, and every local worktree found only the scaffold, proving the imported `DONE` claim lacked implementation. Recovery added a strict run-ID/fixed-schema collector: remote ownership/type/size and credential-like-content checks occur before transfer; exact digests are rechecked after transfer; a sanitized read-only DGX state snapshot supplies cluster/Cilium/BPF/connectivity evidence; and a new external directory is atomically published with mode-0600 files, mode-0700 directory, manifest, SHA-256 inventory, 72-hour expiry, local residency/encryption boundary, no legal-hold claim, and model-use prohibition. Fixture mode never uses SSH and exercises the same schema/sensitivity boundary. | 2026-08-26: syntax and all five M1B focused suites passed. Negative collection tests refused bearer material, kubeconfig key data, wrong run identity, manifest drift, symlinks, arbitrary remote paths, unsafe IDs, and overwrite attempts without publishing output. Full `make check` passed generated drift, frontend lint/build, vet, build, race tests, and both self-checks; only the known non-fatal macOS linker warning appeared. On `spark-5343`, revision `d0994883c9c4a9a2fe62c361e1e96b5dfa302035` with dirty tree digest `4b6c10bfd241af7467d62266bed844464e62f0710e981333be6a726746e56a0b` produced artifact digest `83f2ed7389ecfab792db98048ca60785fd9aeb4d5731213a1a3f3e02645428cf`; the unprivileged self-check exited 0 with no process residue. Collection `m1b5-20260826-01` published seven checksum-verified evidence files (4,904 payload bytes), passed an independent prohibited-data scan, and recorded manifest digest `6ba3aed96a0504f3c69f774645153d3c460f472d23271e70afd07e255246d2b0`. Pre/post checks showed K3s/node Ready, Cilium/Envoy healthy, identical Cilium-only root attachments IDs 471–481, no CanarySting process/BPF/Kubernetes/policy state, and no run residue. Exact cleanup removed two directories; its second invocation was a successful absent-state no-op. | Preserve this forensic correction rather than replacing it with the earlier unsupported claim. Future M1C/M1D collectors must extend fixed schemas for their narrowly owned BPF/Kubernetes evidence without adding arbitrary paths or secret payloads. |
| M1B.6 Idempotent cleanup | DONE | Remove only declared run-owned state and prove restoration. | Privileged tests must leave no residue. | M1B.3, M1B.5 | Tier B | Run twice; remove test state; preserve product/Cilium; recheck health/connectivity/BPF. | Never detach Cilium or delete unresolved/broad paths. | `scripts/dgx/cleanup.sh`, `scripts/dgx/cleanup_test.sh`, environment doc | `bash -n scripts/dgx/*.sh` and embedded remote program<br>`scripts/dgx/cleanup_test.sh`<br>`scripts/dgx/copy_test.sh`<br>`scripts/dgx/run_test.sh`<br>`make check`<br>build/copy/run/inspect/cleanup twice on `falcon1` | Product area: Development infrastructure. Added a strict run-ID interface resolving only the matching incoming, published-stage, and execution-evidence directories. Cleanup validates host/architecture, ownership, entry types, fixed schemas, stage checksums, evidence run identity, and process absence before any removal; it refuses arbitrary/broad/historical/privileged cleanup. Kubernetes, BPF, containers/images, system settings, product deployments, and historical artifacts remain explicitly excluded. `--inspect` and `--dry-run` separate observation from mutation; every normal run invokes the read-only DGX checker. | 2026-08-26: local syntax and focused cleanup/copy/run compatibility tests passed, including embedded-remote-script parsing and unsafe input refusals; `make check` passed generated drift, frontend lint/build, fmt, vet, build, race tests, and both self-checks (with the known non-fatal macOS linker warning). An ARM64 engine artifact from revision `9bf32de7f9926cf49e5710f95531882dad59ce76` and dirty tree digest `17357b50a16da0de90977577ed7289a6b5adb635b8b5d7ebf96f31807e8d4d7f` was checksum-verified at `/var/tmp/canarysting/m1b6-20260826-01`; the unprivileged self-check exited 0 with no process residue and wrote matching execution evidence. Read-only cleanup inspection validated both directories. The first cleanup removed exactly the stage and evidence; the second reported the root absent and succeeded as a no-op. Both post-checks showed K3s/node Ready, Cilium/Envoy healthy, the same 11 Cilium-only root attachments (IDs 471–481 as inventoried), no CanarySting process/BPF/Kubernetes state, and no matching historical artifact. The local build fixture was also removed. An initial remote parser defect failed before deletion; the corrected script added a permanent embedded-program syntax regression check before the successful proof. | Generic cleanup deliberately stops at M1B filesystem state. M1C/M1D and future deployment/attacker workflows must add their own narrowly owned BPF/Kubernetes/process cleanup evidence instead of broadening this command. The historical image/temp-artifact note remains outside this task and requires separate authorization if it reappears. |
| M1B.7 Document workflow | DONE | Document roles, facts, safety, cleanup, tiers, and hardening. | Future Codex sessions need the same operating model. | S0 | Tier A plus read-only DGX facts | No secrets; observations distinguished; kubeconfig risk highest priority. | Mac canonical; least privilege; cleanup evidence. | `docs/DEVELOPMENT_ENVIRONMENT.md` | doc/secret review; compare checker output | Risks are backlog, not implicit repairs. The CanaryPlatform correlation-lab and attacker boundaries were added in the 2026-08-26 architecture bootstrap without replaying this task. | Created in S0 from 2026-08-24 inspection; completion state and evidence preserved. | Security corrections carried forward to M8. |

## Milestone 1C: cookiespike integration proof

| Task | Status | Objective | Why it matters | Dependencies | Required validation tier | Acceptance criteria | Relevant architecture constraints | Likely files | Validation commands | Implementation notes | Completion evidence | Follow-up work discovered |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| M1C.1 Socket-cookie join proof | DONE | Repeatedly prove capture and deterministic workload/flow correlation without enforcement. | Socket cookie is the sole L7/kernel join. | M1B.2–M1B.6 | Tier B | Mac ARM64 build; safe flow resolves nonzero cookie/identity; missing attribution fails; explicit PASS/FAIL; no residue; repeat passes. | Observe-only; no alternate join; minimal attach scope. | cookiespike, sockops, Envoy identity, DGX fixture/script | focused tests; cross-build; pre/post checks; two runs; cleanup | Product area: CanarySting. Validation tier: DGX integration. `cookiespike -proof` loads the production sockops resolver on one supplied child cgroup, creates one loopback TCP flow, reconstructs the Envoy remote-to-local tuple, compares the resolved nonzero cookie with an independent `SO_COOKIE` oracle, creates the contract `FlowIdentity`, proves missing attribution and checked close-delete, and never loads enforcement. The harness re-verifies its checksum-bound stage and fails closed unless `/var/tmp` is a root-owned mode-1777 directory. A privileged helper opens the user-owned artifact once, copies that descriptor to an exact root-owned mode-0700 snapshot directory, verifies size and SHA-256, locks the copy to mode 0500, and executes only the snapshot. It observes the BPF program live only on the exact child, compares full program/map/link and root-attachment inventories, bounds logs, validates exact evidence, and cleans only its run-owned cgroup, snapshot, evidence, and stage. Recovery cleanup remains physically anchored, quarantine-based, exact, and idempotent. All earlier Gate 3 findings—especially verification-to-use binding and final-tree repeatability—are remediated. | 2026-08-26: shell syntax, focused checksum/race/cleanup regressions, all DGX harness suites, generated drift, frontend lint/build, vet, build, race tests, both self-checks, and `make check` passed; only the known non-fatal macOS linker warning appeared. Diagnostic run `m1c-g3-20260826-01` failed closed because `/run` is `noexec`, then exact and absent-state cleanup passed. Runs `-02` and `-03` passed but were superseded when the final tree added the trusted-parent assertion. The final ARM64 artifact SHA-256 was `d24549f0807713fc3a7105e75e4d15795e2d96d5c0d86996a038537d2edb8c3a`, from revision `bcfba4ed6e5b5f211f46197f1cf455bd2b9a080d` plus dirty-tree digest `9495d60f474605bace378b7ec15d72e32a2c695636af916398eaa2b6ef2f685c`. Independent final-tree runs `m1c-g3-20260826-04` and `m1c-g3-20260826-05` each passed root-owned snapshot verification/cleanup, exact-child attachment observation, missing-attribution refusal, tuple direction, nonzero contract identity matching `SO_COOKIE`, checked close-delete, unchanged root attachments and full BPF inventory, and zero process/BPF/cgroup/snapshot residue. Both inspections passed; root-attachment hash remained `019ec96a0c051d9a93ca4e246e539ef6fcc068a5e49903eb9ecbc41d7753bef0` and program/map/link inventory hash remained `780ad124b49961a6d1f1d292c8aab7829948c23089a6eba27e4a45c05a881346`. Each exact cleanup and absent-state rerun passed; K3s stayed Ready, Cilium/operator/Envoy stayed healthy, and no run artifact remains. | This bounded child-cgroup loopback proof does not claim pod/CNI traversal or live Envoy/mesh workload identity. Those are later DGX Kubernetes/correlation tasks (M2B.2 and M3.2); no fallback join or identity was introduced. Stable stale-tuple reuse is a pre-existing production-guard limitation not exercised by this fresh-map proof and requires separate architecture/test work before broader freshness claims. |

## Milestone 1D: enforcespike Cilium coexistence proof

| Task | Status | Objective | Why it matters | Dependencies | Required validation tier | Acceptance criteria | Relevant architecture constraints | Likely files | Validation commands | Implementation notes | Completion evidence | Follow-up work discovered |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| M1D.1 Precise enforcement proof | DONE | Prove one cookie-scoped target is enforced while Cilium/bystanders remain healthy. | A jailed bystander is critical failure. | M1C.1 | Tier B | Record before-state; minimal attach; target-only enforcement; fail-open/bystander proof; detach/remove; connectivity restored; repeatable PASS/FAIL. | Touch-only, precise cookie, observe-before-enforce, never replace Cilium. | enforcespike, enforce/sockops, containment, DGX script | tests/cross-build; Cilium pre/post; target/control; BPF cleanup; repeat | Product area: CanarySting. Validation tier: DGX integration. Added a bounded `enforcespike -proof` using two independently attributed loopback sockets in one run-owned child cgroup and one shared listener/destination. It validates each server cookie against `SO_COOKIE` through the production stale guard, proves target and control traffic before enforcement, emits an explicit synthetic proof-canary-touch marker, programs only the target cookie through `containment.Apply`, requires target drop counters plus a same-destination control verdict-map miss/round trip, releases the target, and proves the same connection recovers. The fixed DGX harness binds root execution to manifest-captured artifact identity, pins all operations to one host, observes real sockops/egress/release programs only on the child, asserts K3s/node/Cilium readiness while attachments are live and after detach, fails closed on BPF inventory errors, compares root attachments and complete BPF IDs, bounds evidence, enforces a 24-hour expiry, supports read-only inspection and failed-run recovery, and performs anchored quarantine-based exact idempotent cleanup. Evidence lifecycle is documented in `docs/DEVELOPMENT_ENVIRONMENT.md`; model use is prohibited and no customer/Kubernetes payload is retained. | 2026-08-26: final `make check` passed generated drift, frontend lint/build, every DGX harness suite, fmt, vet, build, the full race suite, and both self-checks (only the known non-fatal macOS linker warning appeared). The final Linux/ARM64 artifact from revision `22a650df9f0485606018d528d2151122172b267c`, dirty-tree digest `ea403b1ad4fce2db2a87b064a379c179360b5620e566e70ed51ede811efb9cee`, had SHA-256 `979595d99334f12b69953dbc9190aff74fd6282da8636c0e751c8620df09de6f`. Independent runs `m1d-20260826-03` and `m1d-20260826-04` each proved cookie-0 refusal, observe-before-enforce, explicit touch-only targeting, target-only jail (2 packets/160 bytes dropped), bystander map-miss fail-open, same-connection restoration after release, and child-only live attachments for `canary_sockops`, `enforce_egress`, and `enforce_release`. Both inspections passed. Root attachment hash stayed `019ec96a0c051d9a93ca4e246e539ef6fcc068a5e49903eb9ecbc41d7753bef0`; complete program/map/link inventory hash stayed `53ede4a1b9243f437dc31347169d2dabd76c769e165671d5a1d8aa84b01816f7`. Exact cleanup and absent-state reruns passed for both IDs; K3s/node remained Ready, Cilium/operator/Envoy remained healthy, the same 11 Cilium root attachments remained, and no CanarySting process, BPF, cgroup, snapshot, evidence, stage, or Kubernetes residue remained. Diagnostic `-02` proved the datapath but failed its observer on incorrect generated program-name expectations; the corrected final runs above and recovery cleanup supersede it. 2026-08-27 Gate 3 remediation bound the privileged snapshot to manifest metadata, made inventories and supporting checks fail closed, pinned the DGX host, moved target/control to one destination, replaced inherited file limits with bounded pipe draining, moved the Cilium sample behind an enforcement-active traffic handshake, bound quarantine deletion to the validated directory inode, made inspection verify the complete schema/log digests/inventory equality, and scheduled expiry deletion from reboot-cleared runtime tmpfs. The final remediation artifact from revision `8c374aa1016e93a99c8784ea617fc21b01b5d17e`, dirty-tree digest `880bacfe79db6cc69a3e67381cc3e57b7415ad308739893e8564207511cb092e`, had SHA-256 `3d05a305fce54aa81e2bb5c1ae70130c4b1d477719673128816d080d5bc36662`. Run `m1d-pr94-g3c-20260827-03` passed the enforcement-active health handshake, strict inspection with an active expiry timer, exact timer/evidence/stage cleanup, and fail-closed post-checks with no process, BPF, cgroup, snapshot, evidence, stage, or Kubernetes residue. Run `m1d-pr94-g3c-20260827-05` then directly fired the corrected transient expiry service and proved scheduled evidence deletion before exact stage cleanup and another residue-free post-check. The preceding `-04` proof passed but exposed systemd environment expansion inside the first expiry command; it failed safely, was exactly cleaned, and the corrected service now disables that expansion. Run `-02` independently passed the same code-path handshake, inspection, and idempotent cleanup before evidence residency moved to tmpfs; the safe failed `-01` mode check was exactly cleaned. Follow-up Gate 3 remediation made both node-health queries preserve producer status, activated a retrying expiry timer before evidence creation, held the validated directory descriptor through manual and scheduled deletion, made timer cancellation fail closed and verify unload, derived expiry from one completion instant, declared the complete dry-run mutation boundary, and published health acknowledgements atomically. Artifact tree digest `b6d274d0e2fde40594a1f6c02da4798d47633a281ebd0865111980552a2dedb3` retained the same 5,243,183-byte artifact SHA-256 `3d05a305fce54aa81e2bb5c1ae70130c4b1d477719673128816d080d5bc36662`. Run `m1d-pr94-g3d-20260827-06` passed proof, inspection, retry-property inspection, descriptor-held Linux cleanup, absent-state cleanup, and residue-free health checks. Run `-07` passed proof and inspection, then a direct service start proved the revised scheduled descriptor-held deletion before exact timer/stage cleanup and a residue-free post-check. | Feed the proven child-cgroup multi-attach strategy into M3.1 DaemonSet design. This host-local loopback proof does not claim pod-veth/CNI traversal or a full canary-touch-to-verdict Kubernetes slice; those remain M3.3–M3.6. |

Final 2026-08-27 M1D Gate 3 evidence-root remediation held both the evidence-root and exact run-directory descriptors through manual and scheduled deletion, verified both unlinks, kept the retrying expiry path active until manual deletion succeeded, made an already-absent shared root an idempotent expiry success, and selected the cleanup portability shim by operating system. Runs `m1d-pr94-g3e-20260827-08` and `-09` passed proof, inspection, descriptor-held Linux manual cleanup, exact timer/stage cleanup, and residue-free checks. Run `m1d-pr94-g3e-20260827-10` passed proof and inspection; a direct service start then proved scheduled root-and-leaf-descriptor deletion, and exact cleanup reported evidence absent, unloaded the units, removed the stage, and passed residue-free checks. The staged artifact manifest recorded revision `dfc94b9c0536cdf480a233947143ea1e52ba5873` plus dirty-tree digest `6ef2dbae8a53df59b62de249b6c2383cb157b31b60acbf5d517a63742cc6e18d`; the unchanged artifact SHA-256 remained `3d05a305fce54aa81e2bb5c1ae70130c4b1d477719673128816d080d5bc36662`.

## Post-meeting roadmap authority

The 2026-08-31 product re-baseline changes the sequence and commercial posture of **unstarted** work only. It preserves every status and evidence record above: M1C and M1D remain `DONE` option-preserving technical proofs, and DI.2 remains `DONE` with its recorded remote/CI evidence. No completed task is replayed, reset, or reclassified.

The authoritative sequence after M1D is:

1. **M2A.0 — CanaryView storage and data lifecycle:** review data classes, retention, federated raw evidence, normalized storage, deletion propagation, residency, legal hold, model-use authorization, synthetic isolation, and cost controls.
2. **M2A — Common evidence model and operator contracts:** implement the minimum `Observation`, `Evidence`, identity reference, `PolicyDecision`, provenance, confidence, `SecurityCase`, `Explanation`, `ImpactAssessment`, `Recommendation`, and `ActionPlan` contracts.
3. **M2B — Connector framework and Wave 0 collectors:** establish the read-only collector/capability/health seam and onboard CanarySting, Envoy, Cilium/Hubble, Kubernetes, kernel/eBPF, NGINX, and OpenTelemetry only as individually verified sources. The current detailed M2B.1–M2B.2 rows are the implementation decomposition; M6A later productizes/certifies the Wave 0 portfolio rather than creating a second framework.
4. **M2C — Cross-tool correlation and first security trace:** build partial traces, identity resolution, NAT/proxy translations, evidence linkage, correlation confidence, conflicts, missing evidence, and the first operator-readable trace alongside backend work. The current detailed M2B.3–M2B.5 rows map here.
5. **M2D — CanaryAttacker ground-truth laboratory:** use bounded DGX Ollama/Qwen scenarios and deterministic replay to measure trace completeness, precision, recall, identity accuracy, time alignment, missing observations, and incorrect joins. The current detailed M2C and M2D rows jointly decompose this laboratory; their `TODO` status is unchanged.
6. **M2E — Evidence-grounded agentic insights and operator workflow:** deliver read-only explanation, impact, recommended next step, action preview, approval boundary, validation plan, and rollback explanation with the visual workflow primary. Initial operations are `explain_trace`, `explain_path`, `summarize_case`, `identify_missing_evidence`, `identify_conflicting_evidence`, `assess_impact`, `find_dark_reachability`, `find_canary_opportunities`, `recommend_next_step`, `generate_action_preview`, `explain_expected_impact`, and `explain_rollback`.
7. **M2F — Intelligence graph:** implement `OBSERVED`, `PERMITTED`, `DENIED`, `ADVERSARIAL`, and `ENFORCED` relationships while preserving `DARK REACHABILITY = PERMITTED - OBSERVED`. The current detailed M2E rows map here.
8. **M2G — Optional canary placement intelligence:** produce evidence-backed, operator-approved recommendations beginning with honeytokens, credentials, routes, API endpoints, data objects, synthetic identities, and external decoys; Kubernetes service canaries are one later form. The current detailed M2F rows map here.
9. **M3 — CanarySting deployment profiles:** implement optional forms in increasing customer-side footprint: M3A SaaS-managed external/identity canaries; M3B Site Gateway-assisted private canaries; M3C proxy/route integrations; M3D Kubernetes operator/service canaries; M3E local response/eBPF. The current detailed M3 rows map to M3D/M3E and do not make Kubernetes universal.
10. **M4 — Operator experience expansion:** expand Operations, Incidents, Flows, Canaries, Actions, and Integrations from the M2C/M2E slices; UI does not wait for all backend work.
11. **M5 — Structured agent interface:** start read-only; write operations wait for authorization, preview, approval, validation, and rollback contracts.
12. **M6 — Category-based connector expansion:** use Wave 0 reference certification, Wave 1 cross-vendor proof, Wave 2 enterprise breadth, Wave 3 context/operations, and connector operations.
13. **M7 — Cross-vendor action coordination:** CanaryView expresses security intent; native control planes execute separately authorized vendor-native `ActionPlan` objects.

The detailed future rows below are retained as status-preserving implementation decomposition and historical planning context. Where an old milestone heading or dependency implies Kubernetes-first, graph-before-agentic-work, or UI-after-backend sequencing, this section supersedes it. The reason is the meeting evidence: reduce customer-side footprint, lead with standalone CanaryView value, make evidence-grounded insight visible early, and retain Kubernetes/CanarySting as strategic options rather than prerequisites.

## Roadmap migration record

All future tasks in the previous M2–M5 plan were `TODO`; no completion state is discarded. The earlier Kubernetes-pivot migration remains useful implementation context and is further re-mapped by the post-meeting authority above:

- Previous Kubernetes vertical-slice and control-plane work maps specifically to optional M3D/M3E, including deployable assets, least privilege, CRDs, identity, scope defenses, desired/actual/idempotent reconciliation, selectors, envtest, placement, touch, verdict, eBPF, audit, UI, and no-touch restraint.
- Previous graph work maps to M2F and keeps the existing OBSERVED-store reuse, read-only PERMITTED ingestion, canary-derived ADVERSARIAL rule, dark reachability, provenance, dashboard exposure, and no-auto-enforcement constraints.
- Previous production hardening moves to M8, after the CanaryPlatform action layer, without being prematurely implemented.
- The first M2C trace and M2E agentic/operator slices start the real operator experience early. M4 expands it; user-interface work does not wait until M4 to begin.

## Milestone 2A.0: CanaryView storage and data-lifecycle architecture

Goal: review how CanaryView stores, expires, protects, reconstructs, and learns from security intelligence before canonical-model implementation. This is an architecture gate; it does not implement or select a production backend.

| Task | Status | Product area | Objective | Why it matters | Dependencies | Required validation tier | Acceptance criteria | Relevant architecture constraints | Likely files | Validation commands | Implementation notes | Completion evidence | Follow-up work discovered |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| M2A.0.1 Inventory and classify persistent data | DONE | CanaryView / Shared platform | Review the current bbolt buckets, audit/events, topology/deviants/L7 state, NDJSON spools, raw references, feature state, and every planned persistent data class. | A production lifecycle cannot be designed from assumptions or by relabeling Sting stores. | M1D.1 | local | Current behavior/caps/TTLs/cleanup/gaps are code-grounded; planned classes have sensitivity and owner; no secret content is copied. | Rule 5/9; federated evidence; retain intelligence longer than telemetry. | `docs/CANARYVIEW_STORAGE_AND_RETENTION.md`, persistence/package inventory | source/link review; inventory completeness checklist; `git diff --check` | Product area: CanaryView Core with shared-platform lifecycle governance. Deployment profiles: all four; Profile 1 is the lowest applicable profile and no local component was introduced. Validation tier: local. PR risk: LOW (documentation-only inventory; no remote profile). Inventoried current bbolt, baseline/malicious/events, topology/deviants/triage/L7/audit, file-spool, SIEM/demo, projection, in-memory feature/privacy, and eBPF lifecycle behavior; assigned all planned classes an owner and sensitivity. No runtime/storage behavior, backend, dependency, collection authority, credential authority, model-use authority, or DGX state changed. | 2026-08-31: source anchors and the document completeness checklist passed; `git diff --check` passed. Focused persistence/lifecycle package tests passed (the SIEM/dashboard loopback tests were rerun with normal local loopback after the workspace sandbox refused listener creation). Final `make check-fast JOBS=4` passed 5/5 in 0.25s at `.test-artifacts/gates/20260901T015903.160268000Z`. Final `make check-pr-local JOBS=4` passed 11/11 in 25.15s at `.test-artifacts/gates/20260901T015908.729366000Z`, with LOW/non-executable risk and no failures, blocks, skips, or safety stops. No DGX qualification was required or run. | M2A.0.2 must decide enforceable expiry/hold/deletion policy for currently unbounded baseline, malicious, event, audit, and triage state; make enriched-touch expiry independent of optional SIEM; and define snapshot/reference/payload rules. M2A.0.3 must decide spool delivery lifecycle, tenant/region/key boundaries, feature/model governance, and any production backend after review. |
| M2A.0.2 Profiles, evidence federation, and lifecycle policy | DONE | CanaryView / Shared platform | Review Lean/Standard/Regulated profiles, live/replay windows, raw-reference/snapshot rules, request-body/canary-secret handling, expiration, legal hold, and derived deletion/invalidation. | Data must have an enforceable lifecycle before a canonical type can persist it. | M2A.0.1 | local | Every class defines retention, expiry, hold, deletion path, derived-data effect, source/observed times, and minimum-snapshot behavior; Standard defaults are accepted or explicitly revised. | Raw remains source-owned by default; secrets/payloads off by default; hold does not grant model use. | Storage/retention, data-model, operator docs | profile matrix review; lifecycle/deletion scenario tables; privacy review; `git diff --check` | Product area: CanaryView Core with Shared platform lifecycle governance. Deployment profiles: all four; Profile 1 SaaS-only is the lowest applicable profile and no local component is introduced. Validation tier: local. PR risk: LOW because the changed paths are documentation-only; no remote profile or DGX use. Operator job: select a profile and understand/approve retention, overrides, holds, and deletion consequences. Interaction budget: consequences visible on the profile screen; at most one preview plus one confirmation for a profile/hold change; object lifecycle detail one click away. Accepted concrete Standard defaults, per-class lifecycle clocks, exact hold/deletion behavior, raw-reference states, minimum-snapshot triggers/content, and deletion/invalidation scenarios without implementing a UI, backend, or runtime behavior. | 2026-08-31: the storage policy matrix covers all 17 planned data classes and the lifecycle/deletion scenario and privacy rules were reviewed across storage, canonical-model, operator-experience, and platform-architecture documents. `git diff --check` passed. Final `make check-fast JOBS=4` passed 5/5 in 0.48s at `.test-artifacts/gates/20260901T022342.471771000Z`. Final `make check-pr-local JOBS=4` passed 11/11 in 27.36s at `.test-artifacts/gates/20260901T022349.071835000Z`, with LOW/non-executable risk and no failures, blocks, skips, or safety stops. No DGX qualification was required or run. | M2A.0.3 must decide legal-hold RBAC/review cadence, tenant/region/key and backup boundaries, backend deletion/retry objectives, graph reconstruction/compaction, model-use enforcement/rebuild thresholds, per-class cost/volume estimates, and the supported advanced-override catalog. M2A.0.4 remains the explicit architecture approval gate. |
| M2A.0.3 Logical storage, governance, and cost model | DONE | CanaryView / Shared platform | Review edge spool, evidence refs/snapshots, normalized store, append-only relationship history, case/audit store, feature/model registry, residency/encryption, model use, synthetic isolation, and volume/cost estimation. | The graph must be rebuildable, and storage/model authority must be understandable and bounded. | M2A.0.2 | local | Logical layers and truth/rebuild boundaries accepted; tenant/region/key boundaries recorded or explicitly open; operational retention is separate from model use; cross-tenant opt-in and synthetic isolation explicit; estimation inputs/outputs defined. | No graph-only truth; per-tenant learning first; no production DB selection by implication. | Storage/platform/operator docs, architecture-decision records | threat/privacy/cost/reconstruction review; link checks; `git diff --check` | Product area: CanaryView Core with Shared platform storage governance. Deployment profiles: all four; Profile 1 SaaS-only is the lowest applicable profile and no local component is introduced. Validation tier: local. PR risk: LOW because all changes are documentation-only; no remote profile or DGX use. Operator job: understand and approve storage, residency, hold, model-use, and cost consequences. Interaction budget: profile consequences visible without navigation; at most one preview plus one confirmation for a policy change; lifecycle detail one click away. Accepted the layer-authority matrix; append-only graph reconstruction/compaction contract; tenant/residency/purpose-key and same-cell backup/restore boundaries; legal-hold roles, separation, and review cadence; separate default-off model-use grants; zero-tolerance default rebuild gate; synthetic isolation; bounded override catalog; deletion objectives; and auditable cost inputs/outputs. No engine, runtime, dependency, collection, credential authority, customer footprint, or DGX state changed. | 2026-09-01: threat/privacy/cost/reconstruction review and local Markdown target validation passed; `git diff --check` passed. `make check-fast JOBS=4` passed 5/5 in 0.50s at `.test-artifacts/gates/20260901T161920.509543000Z`. `make check-pr-local JOBS=4` passed 11/11 in 26.27s at `.test-artifacts/gates/20260901T161927.922651000Z`, with LOW/non-executable risk and no failures, blocks, skips, or safety stops. No DGX qualification was required or run. | M2A.0.4 remains the explicit architecture approval gate. Later explicitly approved tasks must select/measure production engines, regions/DR pairs, physical tenancy/CMK options, backend deletion mechanics, connector snapshot manifests, identity-provider role mappings, per-class capacity/latency/rebuild/cost targets, and any justified nonzero model-removal tolerance. M2B collectors must expose the accepted per-class counters and size distributions. |
| M2A.0.4 Architecture review gate | DONE | Shared platform | Record approval or required revisions for the platform, data-model, operator-experience, and storage/retention contracts before M2A code. | A reviewed lifecycle contract prevents incompatible model/storage implementation. | M2A.0.3; explicit architecture review | local | Review outcome and unresolved questions recorded; M2A dependencies updated; no backend/runtime implementation; all docs mutually consistent. | Architecture docs define boundaries; plan controls execution; AGENTS invariants unchanged. | `docs/CANARY_*`, `docs/CANARYVIEW_*`, `docs/CANARYPLATFORM_*`, `docs/DEVELOPMENT_PLAN.md` | doc/link/status/consistency checks; `git diff --check` | Product area: Shared platform. Deployment profiles: all four; Profile 1 SaaS-only is the lowest applicable footprint. Validation tier: local. PR risk: LOW because this is documentation-only architecture review. Operator job: understand and approve retention, hold, deletion, residency, model-use, and cost consequences within the existing one-preview/one-confirmation and one-click lifecycle-detail budgets. Approved the federated-evidence, immutable/versioned observation, append-only relationship-history, rebuildable-projection, tenant/residency/purpose-key, exact lifecycle-lineage, hold/RBAC, separate model-use, synthetic-isolation, deletion/restore, cost-estimation, and operator-workflow contracts. Corrected the Standard replay summary to the accepted fixed 72 hours and recorded remaining backend/source/identity/physical-governance choices as later implementation decisions. No DGX use, backend/runtime implementation, dependency, credential authority, collection authority, or customer-side component was introduced. | 2026-09-01: cross-document status, local Markdown target, profile-value, unresolved-question, and consistency checks passed; `git diff --check` passed. `make check-fast JOBS=4` passed 5/5 in 0.51s at `.test-artifacts/gates/20260901T182625.822338000Z`. `make check-pr-local JOBS=4` passed 11/11 in 27.56s at `.test-artifacts/gates/20260901T182632.030906000Z`, with LOW/non-executable risk and no failures, blocks, skips, or safety stops. No DGX qualification was required or run. | M2A.1 is now the next executable task and must resolve package placement, reuse, dependency direction, schema/versioning, lifecycle-envelope ownership, and open questions 1–3 and 12–14 without selecting a production storage engine. |

## Milestone 2A: CanaryView canonical intelligence model

Goal: create the minimum reviewed implementation contracts for cross-tool security intelligence without duplicating existing CanarySting representations.

| Task | Status | Product area | Objective | Why it matters | Dependencies | Required validation tier | Acceptance criteria | Relevant architecture constraints | Likely files | Validation commands | Implementation notes | Completion evidence | Follow-up work discovered |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| M2A.1 Review model boundary and reuse map | DONE | CanaryView / Shared platform | Review the architecture/data-model documents; map `internal/intelligence`, `observebaseline`, identity, dashboard, contract, and audit concepts; decide a minimal package boundary. | A deliberate boundary prevents duplicate sources of truth and circular dependencies. | M1D.1, M2A.0.4; explicit architecture review | local | Record accepted/rejected reuse, package location, dependency direction, schema/versioning decision, and lifecycle-envelope boundary; do not implement until both reviews are recorded. | CanaryView useful without Sting; rule 10; narrow Sting contract remains narrow unless deliberately changed. | Architecture docs, package inventory, `docs/DEVELOPMENT_PLAN.md` | doc/link/import-graph review; `git diff --check` | Product area: CanaryView Core with a Shared platform integration boundary. Deployment profiles: all four; Profile 1 SaaS-only is the lowest applicable profile. Validation tier: local. PR risk: LOW because this review changes documentation only. Accepted `internal/canaryview/model` as the future stdlib-only domain leaf and a separate `canaryview.v1` transport namespace; preserved the narrow Sting contract; mapped accepted/rejected reuse for observebaseline, identity, intelligence/audit/network/transport/SIEM, dashboard, and existing transport patterns; assigned lifecycle value types to the model and enforcement to later services; made `CanaryOpportunity` canonical; and selected one-way outer adapters for source intake and approved Sting delivery. Open questions 1–3 and 12–14 are resolved. No runtime, storage backend, dependency, collection authority, credential authority, customer-side component, enforcement path, or DGX state changed. | 2026-09-01: focused document/reference, package-boundary, stale-decision, and six-question consistency checks passed; `git diff --check` passed. `make check-fast JOBS=4` passed 5/5 in 0.51s at `.test-artifacts/gates/20260901T190108.762675000Z`. `make check-pr-local JOBS=4` passed 11/11 in 27.87s at `.test-artifacts/gates/20260901T190113.576803000Z`, with LOW/non-executable risk and no failures, blocks, skips, or safety stops. No DGX qualification was required or run. | M2A.2 is the next executable task. It must create only the approved envelope/Observation/Evidence/source/scope/lifecycle contracts, import guards, versioned serialization converters, and lifecycle fixtures; it must not select a storage backend or broaden `internal/contract`. |
| M2A.2 Minimal observation/evidence contracts | DONE | CanaryView | Implement only the canonical envelope, immutable Observation, Evidence reference, source/control identity, scope, and approved lifecycle types from M2A.1. | Collectors need a stable vendor-neutral intake seam. | M2A.1 | local | Versioned serialization round-trips; partial fields supported; source/observed times, class/profile/expiry/hold/residency/key reference/model-use/lineage/synthetic fields behave as reviewed; vendor extensions remain evidence; no Sting/runtime redesign. | Provenance and scope mandatory; vendor neutral; observed distinct from derived; no secret/payload default. | Approved neutral package, tests | focused unit/race tests; serialization/lifecycle fixtures; import guards; `make check` | Product area: CanaryView Core. Deployment profiles: all four; Profile 1 SaaS-only is the lowest applicable profile and no local component was introduced. Validation tier: local. Fix-forward PR risk: HIGH because post-merge adversarial review demonstrated failures in the canonical payload, lifecycle, synthetic-isolation, and observation-semantics boundaries; no remote profile or DGX use is relevant. Reopened after PR #104 and removed free-form diagnostics; required explicit source-observation and production/synthetic classification; bound retention to a trusted clock and approved default profile period; changed raw references to platform-generated opaque SHA-256 IDs; advanced the fixture schema to v2 while refusing unsafe v1 invention; and expanded dependency guards across the explicit CanarySting runtime/source roots without implementing M2A.3. No storage backend, protobuf transport, network surface, credential/collection/action authority, runtime integration, customer footprint, or operator workflow changed. | 2026-09-01 fix-forward: focused `go test -race ./internal/canaryview/model`, vet, formatting, and diff checks passed. `make check-fast JOBS=4 RISK=HIGH` passed 10/10 in 4.97s at `.test-artifacts/gates/20260901T200606.005730000Z`; `make check-pr-local JOBS=4 RISK=HIGH` passed 15/15 in 31.47s at `.test-artifacts/gates/20260901T200615.757935000Z`; neither had failures, blocks, skips, or safety stops. Formal Gate 3 passed with no findings on exact implementation head `5bcfdc76373be11fda2c5ab57fc3ad553920c796`. PR #105's attached STANDARD [run 33553472932](https://github.com/henleda/canarysting/actions/runs/33553472932) and manual-increase HIGH [run 33553543978](https://github.com/henleda/canarysting/actions/runs/33553543978) both passed on that head; HIGH Level 2 completed in 16m20s and its authoritative aggregator passed within budget. Privileged eBPF, DGX, Level 3, and Level 4 were risk-selected skips because the change did not touch those surfaces. | M2A.3 is next. It owns broader assertion modes, producer classification, confidence components, verification evidence, missing/conflicting-evidence semantics, and acyclic transformation provenance. M2A.4 owns cross-scope store behavior and production-path rejection. |
| M2A.3 Provenance, confidence, and knowledge state | DONE | CanaryView | Add explicit provenance, confidence components, assertion modes, correlation lineage, missing/conflicting evidence, and producer classification. | Operators and agents must know why a claim exists and how certain it is. | M2A.2 | local | Observation/correlation/inference/recommendation/action cannot be confused; syntactic IDs cannot assert verified; lineage is acyclic and serializable. | Mesh-first identity; AI transparency; no false verification. | Canonical model, identity adapters, tests | table/property tests; forged/parse-only identity negatives; `make check` | Product area: CanaryView Core. Deployment profiles: all four; Profile 1 SaaS-only is the lowest applicable profile and no local component was introduced. Validation tier: local. PR risk: HIGH because this changes canonical knowledge and verification semantics. Implemented mandatory schema-v3 knowledge state/assertion/producer metadata; non-numeric confidence components with explicit time uncertainty; typed missing and contradictory evidence; evidence-backed deterministic/operator verification; assertion-bearing entity references whose syntactic constructor is only `DECLARED`; and exact transformation provenance with an acyclic serializable ancestry closure tied to envelope lineage. Schema v1/v2 conversion refuses unsafe invention and requires trusted-source re-ingestion. Updated the explicitly assumed normalized-observation storage estimate from 768 to 2,048 bytes; lifecycle, retention, hold, residency, encryption-key reference, model-use, and synthetic-isolation policy remain unchanged. No live identity adapter, runtime behavior, storage backend, network/credential/action authority, customer footprint, or DGX state changed. | 2026-09-01: table/property, parse-only/forged identity, verified-evidence, cycle/back-edge/disconnected-lineage, state-confusion, schema compatibility/round-trip, raw-carrier, immutability, import-guard, race, vet, and full Go unit tests passed. Final `make check-fast JOBS=4 RISK=HIGH` passed 10/10 in 5.16s at `.test-artifacts/gates/20260901T224431.568325000Z`; final `make check-pr-local JOBS=4 RISK=HIGH` passed 15/15 in 30.83s at `.test-artifacts/gates/20260901T224331.027278000Z`. Neither gate reported failures, blocks, skips, or safety stops. No DGX profile was selected because the change is limited to the neutral local model; authoritative risk-selected Level 2 CI remains required before merge, the normal main/nightly Level 3 path supplies broader integration qualification, and Level 4 is not applicable. | M2A.4 owns collection/query bounds, scope-isolated store behavior, lifecycle/invalidation fixtures, and production-path rejection of synthetic records. M2B must add concrete correlation/trace records using this metadata. M3.2 must bind `VERIFIED` identities only from live trust evidence and retains DGX Kubernetes validation. The future external transport must make its own explicit breaking-version decision; this task changes only the canonical record schema. |
| M2A.4 Isolation, bounds, lifecycle, and model fixtures | DONE | Shared platform | Prove scope isolation, lifecycle/deletion metadata, bounded collections, deterministic serialization, and redacted fixtures. | Canonical data must not create a privacy, retention, or resource-boundary failure. | M2A.3 | local | Cross-scope queries/joins fail closed; limits/expiry/hold/deletion propagation explicit; model-use remains separate; synthetic fixtures cannot enter production baselines; fixtures contain no secrets/raw customer data; race tests pass. | Rules 5 and 9; bounded resources; reviewed M2A.0 lifecycle. | Canonical store seam/tests/fixtures | no-bleed/property/race/serialization/redaction/lifecycle/invalidation tests; `make check` | Product area: Shared platform. Deployment profiles: all four; Profile 1 SaaS-only is the lowest applicable profile and no local component was introduced. Validation tier: local. PR risk: HIGH because scope isolation, lifecycle visibility, and production model-input rejection are security boundaries. Implemented a backend-neutral, concurrency-safe in-memory production observation-store proof seam with exact tenant/scope/deployment/residency keys; bounded per-scope collections, queries, and all-or-nothing joins; clock-aware ordinary-query lifecycle disposition; same-scope direct-lineage invalidation lookup; explicit per-tenant baseline authorization; deterministic ordering/serialization; and fail-before-capacity synthetic rejection. No persistence engine, physical deletion claim, runtime/collector integration, dependency, credential/action authority, customer footprint, or DGX state changed. | 2026-09-01/02 local evidence: fixture redaction/execution, scope no-bleed, fail-closed join, bound/capacity, lifecycle/hold/expiry/deletion/invalidation, model-use separation, synthetic rejection, deterministic serialization, focused vet, and race tests passed. `make check-fast JOBS=4 RISK=HIGH` passed 15/15 in 34.29s at `.test-artifacts/gates/20260902T002819.836676000Z`; `make check-pr-local JOBS=4 RISK=HIGH` passed 15/15 in 30.76s at `.test-artifacts/gates/20260902T002938.738600000Z`. Both reported zero failures, blocks, skips, or safety stops. PR #107 exact head `05181c9489629b1c390d8d93bcdb1681480fc2c3` passed [CI run 33575738980](https://github.com/henleda/canarysting/actions/runs/33575738980): HIGH Level 2 local passed in 15m47s, risk classification and the authoritative Level 2 aggregator passed, and privileged eBPF/DGX/Level 3/Level 4 jobs were correctly unselected. The normal main/nightly Level 3 path supplies broader integration qualification; Level 4 is not applicable. | M2B.1 is the next executable task and must implement the read-only collector interface against this seam without selecting the production backend. A later lifecycle/repository task must persist lifecycle decisions, execute recursive invalidation and physical deletion/retry across indexes/caches/backups/projections, and prove restore-ledger behavior; M2A.4 intentionally makes no such completion claim. |

## Milestone 2B: Local correlation foundation

Goal: prove correlation against CanarySting engine/eBPF, Envoy, Kubernetes, and Cilium/Hubble before the M6 enterprise connector ecosystem.

| Task | Status | Product area | Objective | Why it matters | Dependencies | Required validation tier | Acceptance criteria | Relevant architecture constraints | Likely files | Validation commands | Implementation notes | Completion evidence | Follow-up work discovered |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| M2B.1 Collector interface | DONE | CanaryView | Define read-only collector/source capability, checkpoint, replay, health, and lifecycle-bearing normalized-observation output. | Connectors need a common seam without vendor assumptions or unbounded spools. | M2A.4 | local | Partial/error/duplicate/restart behavior deterministic; source-native references/checksums and source/observed times preserved; replay expiry/loss visible; no mutation capability hidden in collector. | Passive value; vendor control planes remain native; M2A.0 lifecycle. | CanaryView collector package/tests | contract, retry, replay/expiry, dedupe, import-guard, race tests; `make check` | Product area: CanaryView Core. Deployment profiles: all four; Profile 1 SaaS-only is the lowest applicable profile. Validation tier: local. PR risk: HIGH because this creates source, scope, replay, and lifecycle-bearing intake boundaries. Implemented a category-neutral immutable descriptor and read-only source contract; bounded one-read runner with no silent retry; platform-digest checkpoint references with exact scope/source/collector binding, expiry, non-regression, and compare-and-swap; typed replay loss/expiry; idempotent identical-duplicate handling and conflicting-content rejection; production synthetic rejection; non-secret health/error classification; and repository/sink seams without selecting persistence. No capability manifest/support claim, credential, mutation/action capability, live source adapter, runtime behavior, customer footprint, dependency, or DGX state changed. Action adapters remain a separate later interface. | 2026-09-02 local evidence: focused contract, preservation, partial/error/restart, sink replay, duplicate/conflict, bounds, scope/source/collector isolation, replay expiry/loss, synthetic rejection, non-regressing checkpoint, secret-error suppression, import-guard, compare-and-swap race, vet, formatting, and diff checks passed. Final `make check-fast JOBS=4 RISK=HIGH` passed 10/10 in 4.54s at `.test-artifacts/gates/20260902T014011.283741000Z`; final `make check-pr-local JOBS=4 RISK=HIGH` passed 15/15 in 29.98s at `.test-artifacts/gates/20260902T014021.060347000Z`. Both reported zero failures, blocks, skips, or safety stops. Automatic risk was STANDARD and the manual HIGH increase was honored. No DGX profile was selected because this task is the neutral local seam. PR #109 merged implementation head `a67084cdf41deeb3f8b313bb961c9a12dfd1d585` as `13eaabd9870f98167427d64cbc5799b12714c90b`. The exact implementation head passed manual-increase HIGH [CI run 33581208417](https://github.com/henleda/canarysting/actions/runs/33581208417): Level 2 local (HIGH), risk classification, and the authoritative Level 2 aggregator succeeded; privileged eBPF, DGX, Level 3, and Level 4 were correctly unselected. The PR merge preceded completion of that HIGH run by approximately three minutes; this completion record was not marked `DONE` until the run passed. The normal main/nightly Level 3 path supplies broader integration qualification, and Level 4 is not applicable. | M2B.2 is the next task and owns concrete CanarySting/kernel/Envoy/Kubernetes/Cilium/Hubble source adapters plus DGX evidence. Later repository/Site Gateway work must implement encrypted cursor resolution and persistent checkpoint/health/spool lifecycle; M6A owns capability manifests, coverage/schema-drift productization, certification, and support claims. |
| M2B.2 Normalize DGX-stack observations | DONE | CanaryView / CanarySting | Adapt existing engine, eBPF/kernel, Envoy, Kubernetes, and Cilium/Hubble evidence to observations. | The first proof should use telemetry already present in the lab. | M2B.1, M1C.1, M1D.1 | DGX integration | Each source emits scoped, provenance- and lifecycle-bearing partial observations; federated raw references/checksums retained; no full payload/body default; no alternate socket-cookie join or adapter decision logic. | Rule 4; thin adapters; observation path reuse; synthetic/customer separation. | Collectors/adapters, existing event/topology sources, DGX fixtures | focused fixtures; lifecycle/redaction tests; local gates; DGX before/after source capture and cleanup | Product area: CanaryView Core with CanarySting reference-source integration. Deployment profile: optional Profile 4 Kubernetes/DGX laboratory only; no new customer-side component. Validation tier: DGX integration. PR risk: CRITICAL because the bounded proof adds an allowlisted DGX build/run profile and exercises scope, provenance, lifecycle, privacy, and socket-cookie boundaries. Implemented a closed seven-source proof catalog and one-way normalizers over the existing CanarySting interaction, `observebaseline` topology, eBPF flow, Envoy request, and Kubernetes metadata views, plus deliberately bounded Cilium/Hubble proof DTOs. Records are scope-keyed and idempotent over immutable source attributes, use HMAC-SHA256 pseudonyms, preserve only opaque raw-reference/content digests, carry explicit schema-v3 provenance/confidence/missing-evidence/lifecycle fields, retain socket cookie as the sole L7/kernel join, and remain model-use-off synthetic records rejected by the production store. A bounded Linux/ARM64 proof command and read-only DGX profile hash then delete raw captures before publishing exactly seven partial observation tombstones and size evidence. No production source binding, capability/support claim, enterprise connector, persistence backend, action authority, alternate flow join, decision logic, payload/body/header, Kubernetes label/annotation, or source mutation was added; M6A retains production connector productization. | 2026-09-02 completion evidence: focused adapter/command race tests, vet, shell syntax, harness contracts, and diff checks passed. Pre-PR `make check-fast JOBS=4 RISK=CRITICAL` passed 18/18 in 23.86s at `.test-artifacts/gates/20260903T010642.452008000Z`; the post-record update repeated 18/18 in 22.65s at `.test-artifacts/gates/20260903T011102.453845000Z`. `make check-pr-local JOBS=4 RISK=CRITICAL` passed 26/26 in 156.00s at `.test-artifacts/gates/20260903T010710.602850000Z`. All reported zero failures, blocks, skips, or safety stops and selected `dgx-harness,dgx-kernel` remote coverage. Exact clean-revision DGX run `m2b2-pr-20260902-01` qualified implementation head `8b07069d1d20bd946f66d20e2b92dc2696912c26` with `source_state=clean` and source-tree SHA-256 `08c9a367789a71b7a0af29120a231352e11b3e52dfe9634dd6729c68ef7459e6`; it passed preflight, seven-source normalization/inspection, healthy before/after checks, raw-capture deletion, exact cleanup, and a second idempotent cleanup on `spark-5343`. It emitted seven records totaling 20,719 canonical bytes with a 2,937–3,002-byte range; all run-owned stage and evidence candidates were absent after cleanup, while K3s/Cilium/Cilium Envoy remained healthy and Hubble Relay/CanarySting absence remained explicit partial evidence. [PR #111](https://github.com/henleda/canarysting/pull/111) merged that implementation as `f8e9d1abc779af563180a5d02b5424a21c5d81bf`. Its exact head passed [CI run 33703327769](https://github.com/henleda/canarysting/actions/runs/33703327769): risk classification, Level 2 local (CRITICAL), selected DGX smoke, and the authoritative Level 2 PR gate succeeded; privileged eBPF, Level 3, and Level 4 were correctly unselected. The normal main/nightly Level 3 path supplies broader integration qualification, and Level 4 is not applicable. | M2B.3 owns cross-source correlation candidates/translations. M6A owns versioned production connector schemas, capability manifests, drift handling, certification, and support claims. A future governed evidence service must replace the proof-only 24-hour filesystem lifecycle before production binding. |
| M2B.3 Correlation keys and translations | DONE | CanaryView | Implement correlation candidates using time, tuple, identity, request/vendor IDs, socket cookie, OTel IDs, and explicit NAT/proxy translations. | Cross-stack journeys rarely share one trace ID. | M2B.2 | DGX integration | Missing keys supported; exact/strong/weak joins distinguished; translations retain both tuples/control; ambiguity and candidate set preserved. | Socket cookie remains Sting's L7/kernel join; confidence explicit. | Correlation package/tests/fixtures | deterministic/property/ambiguous/clock-skew tests; DGX known flows | Product area: CanaryView Core. Deployment profiles: all four; Profile 1 SaaS-only is the lowest applicable footprint, while DGX is validation infrastructure only. Validation tier: DGX integration. PR risk: CRITICAL because this task defines scope, confidence, ambiguity, and translation semantics and adds a bounded DGX proof profile. Implemented an immutable application seam over minimized digests and typed OpenTelemetry IDs with mandatory general/CanarySting source vantages; versioned configurable time/translation windows; interval-based clock uncertainty; finite candidate, translation, hop, and path limits; deterministic exact/strong/weak evidence; explicit missing and rejected keys; all-candidate retention; unique-strongest selection; and translation-path ambiguity. NAT/proxy hops retain both directed tuples, the translating control, source reference, observed time, and traversal direction. Cross-scope or over-bound input fails closed without truncation. Distinct CanarySting L7/kernel/engine vantages accept only an equal socket-cookie digest and reject request/identity/tuple fallback, preserving the sole L7/kernel join. The seam is ephemeral: it does not construct or persist traces, widen schema-v3 `Observation` or `internal/contract`, claim connector support, authorize action, or change a verdict/enforcement path. Added a checksum-bound unprivileged Linux/ARM64 proof that creates two transient loopback flows, reads real `SO_COOKIE`, minimizes every identifier before evaluation, emits eight fixed assertions, and uses exact-run cleanup. | 2026-09-02/03 completion evidence: focused correlation/command race tests, repeated deterministic ordering, namespace, stale-translation, ambiguity, clock-uncertainty, path-overflow, privacy, import-boundary, scope, and socket-cookie-fallback tests passed; focused vet, shell syntax, all ten DGX harness contracts, deterministic ARM64 build, transfer/cleanup negative tests, manifest validation, and diff checks passed. `make check-fast JOBS=4 RISK=CRITICAL` passed 18/18 with zero failures, blocks, skips, or safety stops in 32.14s at `.test-artifacts/gates/20260903T024450.241946000Z`. `make check-pr-local JOBS=4 RISK=CRITICAL` passed 26/26 with zero failures, blocks, skips, or safety stops in 73.57s at `.test-artifacts/gates/20260903T024528.155286000Z`. Initial DGX run `m2b3-20260902-01` qualified dirty source tree SHA-256 `0b1b8e028c709d77d57f58cb816e514bf44f065a118530aaade07f5cf7e911b5` at base revision `e894dff63fa7a35fa7bf0b1150693f9433289662`: preflight/build/transfer, two real loopback flows, `SO_COOKIE`, exact/strong/weak and translation evidence, missing-time rejection, equal-candidate ambiguity, evidence inspection, post-run health, exact deletion of both stage/evidence paths, idempotent second cleanup, and final healthy K3s/Cilium/Envoy plus absent named CanarySting/BPF state all passed on `spark-5343`. Clean merged-revision DGX run `m2b3-main-20260903-01` qualified `1fdf722c98d2491b036543548128b4f5585a5beb` with `source_state=clean` and source-tree SHA-256 `18896bc57ba7c68e6e28440174ef70c32d065270de86357070e8a301a90963e2`; the same bounded proof, evidence inspection, healthy after-state, exact deletion, and idempotent cleanup passed with two real loopback flows and no raw addresses emitted. [PR #113](https://github.com/henleda/canarysting/pull/113) merged implementation head `4d1ecc33d7ebcf5ede88da7e74a7441d30d32ccc` as `1fdf722c98d2491b036543548128b4f5585a5beb`. Its exact head passed [CI run 33788403924](https://github.com/henleda/canarysting/actions/runs/33788403924): risk classification, Level 2 local (CRITICAL), selected `kernel-full` DGX smoke, and the authoritative Level 2 PR gate succeeded; privileged eBPF, Level 3, and Level 4 were correctly unselected. The clean merged-revision `correlation` run supplies the task-specific DGX evidence not included by the conservative CI profile mapping. No DGX configuration, Kubernetes/Cilium/BPF/service/firewall state, dependency, or production data changed. The normal main/nightly Level 3 path supplies broader integration qualification, and Level 4 is not applicable. | M2B.4 owns durable partial trace/conflict construction, lifecycle, invalidation, and alternate-path projection. M6A owns production source-to-key adapters, capability manifests, drift handling, certification, and support claims. |
| M2B.4 Partial trace and conflict construction | DONE | CanaryView | Build evidence-linked traces with ordered hops, policy decisions, conflicts, missing telemetry, completeness, and derivation lineage. | Honest partial traces are more useful than false certainty. | M2B.3 | DGX integration | Repeatable trace IDs; no cross-scope joins; alternate candidates retained; explanation cites each join; bounds/expiry/hold/deletion invalidation enforced; broken raw references remain visible. | Immutable observations; provenance; privacy; reviewed lifecycle. | Trace builder/store seam/tests | fixture/race/no-bleed/expiry/deletion tests; DGX partial/conflict cases | Product area: CanaryView Core. Deployment profiles: all four; Profile 1 SaaS-only is the lowest applicable footprint, and the DGX remains validation infrastructure only. Validation tier: DGX integration. PR risk: CRITICAL because this task introduces lifecycle-bearing trace records, scope/invalidation boundaries, and a bounded DGX proof profile. Implemented an immutable backend-neutral builder with deterministic trace IDs and ordered observation/policy-decision hops; complete correlation alternatives and translation paths; record-cited join explanations; explicit declared coverage, gaps, conflicts, and raw-reference availability; and composite correlation knowledge/provenance. Added a bounded concurrency-safe production-store proof seam with exact-scope reads, hold/expiry visibility, synthetic rejection, and direct-parent deletion/expiry/invalidation overlays without a physical-deletion claim. Persistent-type review: `CORRELATED_TRACE` is Confidential; Lean/Standard final retention is 90 days/13 months from an exact trace-close clock, while Regulated/override expiry must be concrete; exact trace/lineage holds suspend expiry without widening access or model use; parent expiry/deletion invalidates the rebuildable trace projection while source observations remain governed independently; tenant/residency and purpose-key boundaries come from the envelope; per-tenant/cross-tenant model use remains separately explicit and defaults off; provenance directly names every observation, translation, and conflict input; and estimated storage impact remains mandatory. Added redacted partial/conflict fixtures and an unprivileged checksum-bound Linux/ARM64 DGX proof that emits eight fixed assertions for passive/no-canary behavior, deterministic IDs, ambiguity, evidence citations, broken raw references, lifecycle, exact-scope invalidation, and hard bounds. No production backend, source mutation, connector/action authority, CanarySting verdict/enforcement change, or customer-side component is in scope. | 2026-09-03 pre-PR evidence: focused trace/model/command fixtures, deterministic ordering, ambiguity, explicit conflict, evidence citation, verified-identity and translation lineage, broken-reference, passive/no-canary, scope/no-bleed, lifecycle/hold/expiry, direct-parent invalidation, synthetic rejection, capacity/bound, import-boundary, vet, and race tests passed. All eleven local DGX harness contract suites passed, including deterministic Linux/ARM64 builds plus transfer/cleanup negative cases. Final `make check-fast JOBS=4 RISK=CRITICAL` passed 26/26 with zero failures, blocks, skips, or safety stops in 65.40s at `.test-artifacts/gates/20260903T195535.932827000Z`; final `make check-pr-local JOBS=4 RISK=CRITICAL` passed 26/26 with the same zero counts in 92.89s at `.test-artifacts/gates/20260903T195645.174480000Z`. Both selected `dgx-harness,dgx-kernel` remote coverage. Exact dirty-tree DGX run `m2b4-20260903-02` qualified base revision `a87a8bd1bd6bc811b6551472062f97f93f08a183` and source-tree SHA-256 `855afaddf60fcff7755c6f0b24a2b9aaae8a6f0e60a8abb76d7ad39bd8b995dd`: preflight/build/transfer, all eight passive-partial/conflict assertions, evidence inspection, unchanged healthy K3s/Cilium/Cilium Envoy and absent named CanarySting/BPF state, exact deletion of stage/evidence, and a second idempotent cleanup passed on `spark-5343`. The run was unprivileged, opened no sockets, emitted no raw identifiers, and changed no source/Kubernetes/Cilium/BPF/service/firewall state. [PR #115](https://github.com/henleda/canarysting/pull/115) merged exact implementation head `3e7f23f` by squash as `3f2441c`. Its exact head passed [CI run 33817487966](https://github.com/henleda/canarysting/actions/runs/33817487966): risk classification, CRITICAL Level 2 local, selected `kernel-full` DGX smoke, and the authoritative Level 2 PR gate succeeded; privileged eBPF, Level 3, and Level 4 were correctly unselected. Gate 3 returned APPROVE with no blockers after the bounded-work and evidence-schema repair ([review record](https://github.com/henleda/canarysting/pull/115#issuecomment-5533609706)). Clean merged-revision DGX run `m2b4-main-20260903-01` qualified revision `3f2441c0089c3d51b67ee5a47150070bfaf9d047`, `source_state=clean`, and source-tree SHA-256 `41f110e0d49c9b3e2116882f24e33bdad5128f69c20de0da66a25f48a176e94e`: all eight trace assertions, exact evidence inspection, healthy pre/post K3s/Cilium/Envoy state, absent named CanarySting/BPF residue, exact stage/evidence deletion, and idempotent second cleanup passed. This supplies the task-specific `trace` qualification not selected by the conservative CI profile. The normal main/nightly Level 3 path supplies broader qualification; Level 4 is not applicable. | M2B.5 owns the first operator projection; production lifecycle-event persistence, recursive deletion/rebuild, indexes/caches/backups, and backend selection remain later work. |
| M2B.5 First correlated-trace operator slice | DONE | CanaryView / Shared platform | Project one real/fixture-correlated trace into an operator workspace with explanation and evidence. | The interface must evolve with the data model, not arrive after backend completion. | M2B.4, M1A.8 | DGX integration | Operator can see what happened/why/affected state in one workspace; raw evidence is one click; partial/conflicting states are clear; Go-projected live-route Playwright path and frontend lint/build pass. | Unified console; progressive disclosure; display off verdict path. | Dashboard backend/app, trace fixtures/tests | focused API tests; frontend lint/build; Playwright live route/projection; DGX read-only trace smoke | Product area: CanaryView Core / Shared platform. Deployment profiles: all four; Profile 1 SaaS-only is the lowest applicable footprint, and DGX is validation infrastructure only. Validation tier: DGX integration. PR risk: CRITICAL because the work extends the checksum-bound qualification artifact and its exact evidence schema, while the product path itself remains read-only. Implemented a deterministic minimized synthetic trace fixture and pure projection over the canonical M2B.4 trace; a strict trace-ID GET route behind an injected, already-authorized exact-scope query boundary; and a responsive keyboard-operable workspace showing what happened, why, affected identities, source-ordered hops, confidence, missing telemetry, contradictory evidence, citations, lifecycle/model-use state, and opaque source-owned raw references. Explanation is visible without interaction and raw-reference metadata opens in one click without fetching or copying source payload. The production composition root leaves the trace source unwired until scoped authentication/query work exists; the route rejects tenant/scope query selectors rather than accepting caller-selected scope. File-level import and contract tests keep the projection off adapter, BPF, contract, engine, intelligence, persistence, Sting, verdict, tier, and action paths. No persistence class, dependency, source payload, production connector, case/action authority, customer-side component, or CanarySting verdict/enforcement behavior was added. The bounded unprivileged DGX trace proof validates exact Go projection facts in a ninth fixed assertion, binds its 19-line result to the stable `m2b5-operator-conflict` scenario, and retains its exact-run mutation and 24-hour evidence lifecycle. Trace-proof paths select the task-specific `dgx-trace` CI profile. Operator job: explain one cross-stack flow. Expected summary-to-explanation: 0 clicks; source-owned raw-reference metadata: 1 click. | 2026-09-03 pre-PR evidence: focused projection/API/route-rejection, import/decision-field guard, command, race, and harness tests passed; frontend lint/build passed; the original two-case fixture journey and 1440-pixel visual review are superseded by the Gate 3 remediation evidence recorded below. Final `make check-fast JOBS=4 RISK=CRITICAL` passed 19/19 with zero failures, blocks, skips, or safety stops in 17.88 seconds at `.test-artifacts/gates/20260904T004324.117674000Z`; final `make check-pr-local JOBS=4 RISK=CRITICAL` passed 28/28 with the same zero counts in 64.80 seconds at `.test-artifacts/gates/20260904T004349.401067000Z`. Exact dirty-tree DGX run `m2b5-operator-20260903-01` qualified base revision `3f2441c0089c3d51b67ee5a47150070bfaf9d047` and source-tree SHA-256 `966e25039a893b5b147bb1ae87ca93772374001c03b3199f3f529db6b6e6fe25`: preflight/build/transfer, all nine fixed trace/operator assertions, exact evidence inspection, unchanged healthy K3s/Cilium/Cilium Envoy and absent named CanarySting/BPF state, exact deletion of stage/evidence, and an idempotent second cleanup passed on `spark-5343`. The proof was unprivileged, synthetic, emitted no raw identifiers, opened no sockets, and changed no source/Kubernetes/Cilium/BPF/service/firewall state. [PR #116](https://github.com/henleda/canarysting/pull/116) merged exact implementation head `fadf930720d8e4a4769eae3707e7658724edfb46` by squash as `68c82e349f63e12c2ddb6f61191895d04873bcb1`. Its exact head passed [CI run 33925122588](https://github.com/henleda/canarysting/actions/runs/33925122588): risk classification, CRITICAL Level 2 local, selected `dgx-trace`, and the authoritative aggregate Level 2 gate all succeeded; privileged eBPF, Level 3, and Level 4 were correctly unselected. Fresh Gate 3 review approved the exact head with no findings ([review record](https://github.com/henleda/canarysting/pull/116#issuecomment-5547298779)). Clean merged-revision DGX run `m2b5-main-20260904-01` qualified revision `68c82e349f63e12c2ddb6f61191895d04873bcb1`, `source_state=clean`, and source-tree SHA-256 `bd5c35a46886c1b682a59cadf391749fa0a01f11de361166466863fb37336021`: all nine fixed trace/operator assertions, exact evidence inspection, healthy pre/post K3s/Cilium/Envoy state, absent named CanarySting process/BPF/Kubernetes residue, exact stage/evidence deletion, and an idempotent second cleanup passed on `spark-5343`. The normal main/nightly Level 3 path supplies broader qualification, and Level 4 is not applicable. | M4 expands navigation/incidents/actions; M8 wires authentication and the production exact-scope trace query service. |

M2B.4 Gate 3 remediation and closure (2026-09-03): PR #115 review findings were addressed by binding retained correlation results to the exact immutable anchor/candidate/rejection inputs, including scope, source vantage, and synthetic scenario; adding aggregate correlation-work, combined conflict-count, and per-conflict-evidence limits; deriving confidence completeness from missing declared coverage independently of conflict status; carrying synthetic scenario provenance through correlation and trace IDs plus a separate scenario-bound laboratory store; and replacing the permissive DGX evidence check with an exact 19-line schema bound to run, scenario, source revision/state/tree, artifact checksum, log checksums, fixed output, and a validated 24-hour expiry. Focused model/correlation/trace/command race tests and the hardened DGX harness contract passed. Final repair gates passed 26/26 with zero failures, blocks, skips, or safety stops: `make check-fast JOBS=4 RISK=CRITICAL` at `.test-artifacts/gates/20260903T232211.268911000Z` and `make check-pr-local JOBS=4 RISK=CRITICAL` at `.test-artifacts/gates/20260903T232322.270017000Z`; both selected `dgx-harness,dgx-kernel` remote coverage. Independent reviewer verification passed the same 26/26 local PR gate at `.test-artifacts/gates/20260903T234041.592507000Z`. Exact-head authoritative CI, Gate 3 approval, squash merge, and the clean merged-revision task-specific DGX trace proof recorded in the M2B.4 row close the task as `DONE`.

M2B.5 Gate 3 remediation and closure (2026-09-04): PR #116's initial review returned `REQUEST_CHANGES` ([review record](https://github.com/henleda/canarysting/pull/116#issuecomment-5534558234)). The repair preserves same-ID supporting and contradicting evidence contexts, keeps ID plus schema version for canonical record/evidence references, preserves each join explanation's opaque key fingerprint, method-qualified timing, and translation-path identity, leaves source-owned raw references without an invented claim role, projects aggregate and per-hop time uncertainty, labels source-owned raw lifecycle separately from trace lifecycle, and renders conflict evidence references. The trace page now has explicit loading/not-found/unavailable/malformed states, an empty-to-loading-to-ready announcement, and dependency-free runtime validation that enforces closed display enums, calendar-valid RFC3339 timestamps, positive time windows and candidate counts, lifecycle/legal-hold consistency, versioned provenance, distinct full join and canonicalized conflict identities, distinct conflict reference sets, contradictory evidence presence, method-aware optional join timing, translation-path shape, and raw-reference ownership/role/hash invariants. Its zero-retry Playwright gate starts a loopback-only test Go source and the current Next checkout with server reuse disabled, then exercises `tracefixture.OperatorConflict` through `ProjectTrace`, the production backend handler, Next rewrite, browser fetch, and React workspace. Nineteen Chromium cases cover exact projected JSON identity, same-ID/different-schema provenance, same-pair/multiple-match explanation identity, same-kind/same-record/different-evidence conflict identity, a real forward translated-tuple path, time-qualified and non-time-qualified join presentation, order-independent duplicate conflict detection, duplicate and empty inner conflict-reference rejection, explanation/conflict/raw-reference behavior, natural sequential keyboard activation and focus restoration, sixteen terminal failures including invalid JSON and independently mutated semantic violations without concurrent loading text, 320-CSS-pixel document and navigation reflow, and WCAG AA contrast including navigation hints. Go fixture/projection/handler changes now select this browser gate even without a TypeScript edit. The DGX command proves only Go-level facts and rejects any scenario other than `m2b5-operator-conflict`; its fixed operator-projection evidence is derived from the same expected missing/conflict counts used by the proof assertion, the classifier selects `dgx-trace` for these proof paths, and CI now retains and runs every selected bounded DGX profile rather than silently accepting the first match. Final `make check-fast JOBS=4 RISK=CRITICAL` passed 19/19 with zero failures, blocks, skips, or safety stops in 22.06 seconds at `.test-artifacts/gates/20260904T221652.578989000Z`; final `make check-pr-local JOBS=4 RISK=CRITICAL` passed 28/28 with the same zero counts in 129.98 seconds at `.test-artifacts/gates/20260904T221718.875798000Z`. Both selected `dgx-trace`. Exact-head CI, fresh independent Gate 3 approval, squash merge, and the clean merged-revision task-specific DGX trace proof recorded in the M2B.5 row close the task as `DONE`.

## Milestone 2C: CanaryAttacker Qwen harness

Goal: use local Ollama/Qwen on the DGX as a bounded synthetic adversary that emits reproducible ground truth.

| Task | Status | Product area | Objective | Why it matters | Dependencies | Required validation tier | Acceptance criteria | Relevant architecture constraints | Likely files | Validation commands | Implementation notes | Completion evidence | Follow-up work discovered |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| M2C.1 Verify Ollama/Qwen lab state | IN_PROGRESS | CanaryAttacker / Development infrastructure | Inspect Ollama, model, binding, resources, target lab, clocks, and prerequisite telemetry read-only. | Historical model presence is not a durable guarantee. | M2B.5, M1B.6 | DGX attacker/correlation | Exact version/model/binding/resource facts recorded without secrets or mutation; unsuitable exposure/state blocks execution. | DGX privileged lab; no automatic repair/install. | `scripts/dgx/attackercheck*.sh`, `scripts/dgx/pr*.sh`, risk/gate manifest and workflow mapping, Makefile, testing/environment docs, this plan | local syntax/fixture/coordinator/risk tests; `make check-fast`; `make check-pr-local`; passive `falcon1` inspection and `attacker-check` coordinator profile; exact-head CI and Gate 3 review | Product area: CanaryAttacker / Development infrastructure. Deployment profiles: all four as shared validation infrastructure; Profile 1 SaaS-only remains the lowest product footprint and gains no customer-side component. Validation tier: DGX attacker/correlation. PR risk: CRITICAL because this is a fail-closed DGX and attacker-tool safety boundary. The version-2 fixed-host/model inspector bounds the SSH session and individual commands; pins Ollama and curl to HTTP loopback with proxy and curl configuration inputs removed and curl defaults disabled; uses runtime exact-argument allowlists for every sensitive remote operation; discovers every TCP listener owned by one Ollama `MainPID` while that PID and systemd `InvocationID` remain stable across each snapshot; requires the fixed API socket to be in that process-owned set before probing it; then requires the identical service identity and complete listener set after the API/model queries or invalidates all derived evidence and fails closed. It records explicit listener/API/model/GPU error or invalidation states and rejects unsafe or contradictory facts. Target and CanarySting runtime readiness remain `unconfigured` until exact M2C.5 identifiers exist, so cluster-wide names/labels cannot create authority. The local validator recomputes telemetry, safety, endpoint ownership, invalidation relationships, and the exact ordered blocker set. Tests capture and syntax-check the actual remote heredoc, pin the complete reviewed remote body by SHA-256, compare every sensitive reference and call site with a readable manifest, exercise the runtime argument allowlists without sourcing top-level remote code, reject direct sensitive/state-changing and unlisted-executor changes, simulate hostile curl configuration plus service PID/listener turnover during the complete API/model query window, prove alternate-port listener discovery and exact-PID filtering, reject fixed-endpoint ownership mismatches and malformed relationships, and constrain CI to a minimized summary. The coordinator exits before shared preflight-proof creation with `preflight_count=0`, so this profile performs no general BPF feature probe, build, or transfer. The classifier still selects dedicated `dgx-attacker-check` coverage with `requires_live_qwen=false`; no prompt/model execution, automatic repair, secret read, customer component, or product decision-path change is included. | Initial pre-PR local gates and read-only version-1 run passed on 2026-09-06, and exact initial PR head `22dd4b1a6e97d292d9bb2e2d254f7e32266a2592` passed [CI run 34046488647](https://github.com/henleda/canarysting/actions/runs/34046488647), including CRITICAL local, selected DGX, and aggregate jobs. Gate 3 then requested safety hardening in [review comment 5560922937](https://github.com/henleda/canarysting/pull/118#issuecomment-5560922937). Post-repair focused syntax/fixture/coordinator tests, all twelve DGX harness contracts, `go test ./internal/testgate`, and diff checks passed. `make check-fast JOBS=4 RISK=CRITICAL` passed 14/14 with zero failures, blocks, skips, or safety stops in 14.55s at `.test-artifacts/gates/20260906T174953.542283000Z`; `make check-pr-local JOBS=4 RISK=CRITICAL` passed 23/23 with the same zero counts in 53.65s at `.test-artifacts/gates/20260906T175014.032564000Z`. Both selected `dgx-attacker-check,dgx-harness,dgx-kernel`. Direct version-2 inspection and coordinator run `m2c1-g3-20260906-01` both passed at `2026-09-06T17:51Z`: listener/API/inventory probes were successful, binding was loopback-only, the expected model was present with zero loaded models, core telemetry was ready with partial sources, and `live_scenario_ready=false`. The coordinator reported zero preflights, zero builds, and zero transfers. Exact blockers were the unconfigured target, unready Hubble Relay, unconfigured CanarySting runtime, and unimplemented bounded harness. No prompt/model ran, no BPF probe executed, and no secret, raw traffic, or remote artifact was read or created. Subsequent Gate 3 repairs and validation are recorded below. Repaired exact-head authoritative CI, fresh Gate 3 review, and merge evidence remain required before `DONE`. | M2C.2 owns versioned scenario/ground-truth contracts; M2C.3 owns the bounded executor; M2C.5 owns exact target-lab/runtime provisioning and must resolve or explicitly design around the observed Hubble gap. Existing DGX hardening backlog remains separate and requires explicit approval. |
| M2C.2 Scenario and ground-truth contracts | TODO | CanaryAttacker / Shared platform | Define versioned scenarios, targets/budgets, `AttackerIntent`, `AttackerAction`, and evidence lineage. | Evaluation needs pre-action intent and post-action execution truth. | M2C.1, M2A.4 | local | Intent precedes action; denied/failed actions represented; stable scenario/step IDs; serialization, scope, redaction, synthetic classification, and isolation tests pass; production-model ingestion rejects the corpus. | Attacker data is declared synthetic harness truth, not trusted telemetry. | Attacker scenario/contract package, fixtures | schema/golden/no-secret/no-bleed/synthetic-isolation tests; `make check` | Reuse existing attacker budgets/replay where appropriate without runtime redesign. | Pending. | None yet. |
| M2C.3 Bounded tool policy and executor | TODO | CanaryAttacker | Implement reviewed HTTP/DNS/TCP/enumeration/link/fixture-credential/response tools behind executor-enforced policy. | The model must not gain arbitrary host or network authority. | M2C.2 | DGX attacker/correlation | Allowlisted targets only; redirects/DNS rebinding rejected; no shell/K8s/Docker/filesystem tools; hard action/time/byte/concurrency budgets; denials audited. | Safe attacker boundary; designated lab only. | Attacker tools/policy/tests/DGX script | exhaustive allow/deny/timeout/cancel tests; DGX harmless denied/allowed probes; cleanup | Additional tools require explicit review. | Pending. | None yet. |
| M2C.4 Ollama/Qwen bounded loop | TODO | CanaryAttacker | Connect the local Qwen model to structured tools and external budgets/cancellation. | Enables adaptive but controlled scenarios without external model dependency. | M2C.3 | DGX attacker/correlation | Model cannot alter target/policy/budget; every proposal/denial/action logged; stop/cancel deterministic; no arbitrary prompt required by operator. | Model output untrusted; no hidden authority. | Ollama client/planner/coordinator/tests | fake-client deterministic tests; DGX bounded run; process/network inspection; cleanup | Pin/provenance model/version; keep loopback/private. | Pending. | None yet. |
| M2C.5 Reproducible initial scenarios and cleanup | TODO | CanaryAttacker / Development infrastructure | Add enumeration, HTTP probe, harmless lab credential, canary discovery, and canary-touch scenarios with deterministic cleanup. | Small known journeys provide trustworthy correlation fixtures. | M2C.4, M1B.6 | DGX attacker/correlation | Two repeated runs emit stable IDs/intents/actions; targets stay in lab; cleanup twice succeeds; after-state matches baseline. | Canary touch may trigger Sting only under configured safe posture; bounds/precision. | Scenario fixtures, DGX attacker/cleanup/collect scripts | before/scenario/evidence/cleanup/after; negative escape tests; local gates | Separate scenario fixtures from product deployment. | Pending. | None yet. |

Latest M2C.1 repair validation on the current working tree addresses the ambient-curl-configuration, API-socket ownership, service-turnover, and executable-test-extraction findings from the latest [Gate 3 review](https://github.com/henleda/canarysting/pull/118#issuecomment-5561887967). Shell syntax, focused inspector/coordinator tests, `go test ./internal/testgate`, all twelve `make dgx-harness-check` contracts, and `git diff --check` passed. Final `make check-fast JOBS=4 RISK=CRITICAL` passed 14/14 with zero failures, blocks, skips, or safety stops in 5.28s at `.test-artifacts/gates/20260906T203425.723818000Z`; final `make check-pr-local JOBS=4 RISK=CRITICAL` passed 23/23 with the same zero counts in 47.02s at `.test-artifacts/gates/20260906T203437.608714000Z`. Both selected `dgx-attacker-check,dgx-harness,dgx-kernel`. Final direct passive inspection at `2026-09-06T20:35:36Z` and coordinator profile `m2c1-g3c-20260906-02` at `2026-09-06T20:35:49Z` passed with a stable systemd invocation/process-owned all-port listener snapshot, loopback-only binding, fixed socket ownership, successful API/model inventory, expected model present, zero loaded models, core telemetry ready with partial sources, `live_scenario_ready=false`, and the exact four expected blockers. The coordinator recorded zero preflights, builds, and transfers. No prompt/model ran, no BPF probe executed, and no service, Kubernetes, Cilium, firewall, source, secret, raw-traffic, or remote-artifact state was changed or read beyond the fixed report contract. Exact-head CI and fresh Gate 3 approval remain outstanding, so M2C.1 remains `IN_PROGRESS`.

The 2026-09-06 Gate 3d repair addresses the full-query-window identity/listener race and unlisted-executor regression gap from [review comment 5562221979](https://github.com/henleda/canarysting/pull/118#issuecomment-5562221979). The inspector now compares the same process ID, systemd invocation ID, and complete process-owned listener set before and after all fixed API/model queries; any turnover or listener change invalidates API/model evidence and fails closed. The captured remote program is pinned by an exact reviewed SHA-256, and an unlisted Python filesystem-write fixture proves that any otherwise-unmatched executable change is rejected without executing the captured program. Focused shell syntax, inspector/coordinator, test-gate checks, and all twelve DGX harness contracts passed. `make check-fast JOBS=4 RISK=CRITICAL` passed 14/14 with zero failures, blocks, skips, or safety stops in 6.15 seconds at `.test-artifacts/gates/20260906T212635.803622000Z`; the final `make check-pr-local JOBS=4 RISK=CRITICAL` passed 23/23 with the same zero counts in 45.90 seconds at `.test-artifacts/gates/20260906T212909.084957000Z`. Direct passive inspection at `2026-09-06T21:27:49Z` and coordinator profile `m2c1-g3d-20260906-01` at `2026-09-06T21:28:00Z` both passed with stable full-window service/listener evidence, loopback-only binding, successful fixed API/model inventory, the expected model present, zero loaded models, core telemetry ready with partial sources, `live_scenario_ready=false`, and the exact four expected blockers. The coordinator again recorded zero preflights, builds, and transfers. No prompt/model, BPF probe, remote write, service change, source read beyond the fixed contract, or Kubernetes/Cilium/firewall mutation occurred. Fresh exact-head CI and Gate 3 approval remain outstanding, so M2C.1 remains `IN_PROGRESS`.

## Milestone 2D: Correlation quality laboratory

Goal: measure CanaryView correlation against known CanaryAttacker ground truth rather than relying on anecdotal traces.

| Task | Status | Product area | Objective | Why it matters | Dependencies | Required validation tier | Acceptance criteria | Relevant architecture constraints | Likely files | Validation commands | Implementation notes | Completion evidence | Follow-up work discovered |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| M2D.1 Ingest ground truth distinctly | TODO | CanaryView / CanaryAttacker | Ingest scenario intents/actions as a separate declared ground-truth source. | Ground truth must guide evaluation without manufacturing telemetry. | M2C.5, M2B.4 | DGX attacker/correlation | Ground truth never appears as vendor/kernel observation; hints/provenance visible; unmatched steps retained. | Observed vs declared distinction; scope isolation. | Ground-truth collector/trace/evaluator/tests | fixture/no-false-source/no-bleed tests; DGX replay | Report assisted versus unassisted joins if scenario hints are used. | Pending. | None yet. |
| M2D.2 Correlate all initial sources | TODO | CanaryView | Join attacker steps with Cilium/Hubble, Envoy, eBPF/kernel, Kubernetes identity, and CanarySting. | This is the first complete correlation-lab proof. | M2D.1 | DGX attacker/correlation | Each expected source is matched or explicitly missing; joins expose evidence/method/confidence/conflicts; partial traces remain queryable. | No Sting prerequisite for passive hops; cookie join preserved. | Collectors/correlation/evaluator/DGX harness | deterministic fixtures; live scenarios; evidence manifests; cleanup | Do not pull M6 enterprise connectors ahead of the category, manifest, and operator gates. | Pending. | None yet. |
| M2D.3 Correlation metrics | TODO | CanaryView | Calculate completeness, precision, recall, identity accuracy, incorrect joins, missing observations, time alignment, canary detection, and containment precision. | Quality must be measurable and comparable. | M2D.2 | DGX attacker/correlation | Definitions/denominators/confidence thresholds/version recorded; known synthetic errors change metrics correctly; no hidden excluded failures. | Evidence supports every join and metric. | Evaluator/report types/tests | golden/property tests; scenario matrix; repeated DGX runs | Separate telemetry availability from correlation failure. | Pending. | None yet. |
| M2D.4 Evidence-backed laboratory report | TODO | CanaryView / Shared platform | Produce a bounded report and operator trace for each scenario with before/after/cleanup evidence. | Developers/operators need diagnosable PASS/FAIL and coverage gaps. | M2D.3 | DGX attacker/correlation | Explicit PASS/FAIL per criterion; federated evidence links; redaction, expiry, lineage, and synthetic isolation; UI shows completeness/conflicts without raw JSON requirement. | Privacy, provenance, interaction budgets; synthetic corpus separate. | Collector/report/dashboard/DGX artifacts | manifest/redaction/lifecycle checks; Playwright trace path; DGX repeat and cleanup | Record gaps as backlog, not automatic infra repair. | Pending. | None yet. |

## Milestone 2E: CanaryView graph

Goal: implement provenance-bearing `OBSERVED`, `PERMITTED`, and `ADVERSARIAL` reachability using existing Kubernetes/Cilium visibility.

| Task | Status | Product area | Objective | Why it matters | Dependencies | Required validation tier | Acceptance criteria | Relevant architecture constraints | Likely files | Validation commands | Implementation notes | Completion evidence | Follow-up work discovered |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| M2E.1 Decide graph reuse/backend boundary | TODO | CanaryView | Decide whether `observebaseline` is the first backend or feeds a separate representation, then define proxy-neutral scoped graph interfaces and append-only relationship history. | Rule 10 forbids two attribution truths, while historical graph views must remain rebuildable. | M2D.4, M2A.1 | local | Decision documented; imports neutral; relationship assertion/retraction/lineage/expiry/compaction and current/historical rebuild semantics defined; bounded queries and migration path accepted. | Existing OBSERVED source reused; no data-path fork; graph view not sole truth. | Architecture, graph/store seams, import guards | design/reconstruction review; import/dependency/lifecycle tests | Do not recreate retired `internal/topology` or select storage incidentally. | Pending. | None yet. |
| M2E.2 OBSERVED edge adapter | TODO | CanaryView | Expose existing attributed observations with provenance/confidence through the graph boundary. | Preserves working topology and passive value. | M2E.1 | DGX Kubernetes end-to-end | Scope/counts/bytes/direction/protocol/time/TTL/caps preserved; no duplicate collector; DGX known flow matches trace. | Rule 10; observation is not permission. | Observebaseline/graph adapter/tests | adapter/golden/no-bleed tests; DGX trace/graph comparison | Source store remains authoritative for its observations. | Pending. | None yet. |
| M2E.3 Read-only policy ingestion and PERMITTED edges | TODO | CanaryView | Ingest and normalize Kubernetes NetworkPolicy and available Cilium policy visibility to provenance-bearing PERMITTED edges. | Enables predicted blast radius and dark reachability. | M2E.2 | DGX Kubernetes end-to-end | Get/list/watch only; supported selector/default-deny/ipBlock/port semantics correct; ambiguity explicit; source UID/version retained. | Rule 11; label identity lower confidence; no mutation. | `internal/k8s`, graph adapters, RBAC/tests | policy tables; verb/import guard; DGX auth/update/delete fixtures | Additional policy types remain explicit later work. | Pending. | None yet. |
| M2E.4 Canary-derived ADVERSARIAL edges | TODO | CanaryView / CanarySting | Add adversarial edges only from confirmed attributed canary activity. | Prevents anomaly-only hostile labels. | M2E.2, M1C.1 | DGX Kubernetes end-to-end | Canary-touch positive; novelty/non-canary/zero-base negative; cookie/scope/evidence retained; deterministic aggregation. | CanarySting rule 8; precise attribution. | Graph/event adapter/tests | positive/negative/no-touch tests; DGX touch/control | Model/attacker assertions alone cannot create this edge. | Pending. | None yet. |
| M2E.5 Dark reachability and path explanation | TODO | CanaryView | Compute `PERMITTED - OBSERVED`, bounded reachability, attack paths, and evidence-backed explanations. | Reveals valuable unused attack surface. | M2E.3, M2E.4 | DGX Kubernetes end-to-end | Scope/identity/direction/protocol/port/time/uncertainty aware; provenance survives query; property tests and known DGX fixture pass. | Advisory only; observe before enforce. | Graph algorithms/query/tests | table/property/bounds tests; DGX known graph | Unknown/incomparable edges remain uncertain. | Pending. | None yet. |
| M2E.6 Graph operator projection and no-action guard | TODO | CanaryView / Shared platform | Expose edge types, confidence, provenance, dark reachability, and telemetry gaps without any enforcement writer. | Operators need explainable value before deception/action. | M2E.5 | DGX Kubernetes end-to-end | UI/API distinguish observed/permitted/adversarial/inferred; evidence one click; graph changes make no BPF/policy mutation; frontend/Playwright/DGX smoke pass. | Display-only; rule 11; accessibility. | Dashboard/API/import guards/tests | no-writer guard; frontend checks; Playwright; DGX before/after | Human recommendation workflow follows in M2F. | Pending. | None yet. |

## Milestone 2F: Canary placement intelligence

Goal: let CanaryView recommend valuable CanarySting placements conservatively and with human approval.

| Task | Status | Product area | Objective | Why it matters | Dependencies | Required validation tier | Acceptance criteria | Relevant architecture constraints | Likely files | Validation commands | Implementation notes | Completion evidence | Follow-up work discovered |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| M2F.1 CanaryOpportunity contract | TODO | CanaryView / CanarySting | Implement the reviewed target/scope/reason/risk/signal/type/location/evidence/confidence/expiry/constraints contract. | View and Sting need a neutral, explainable recommendation seam. | M2E.6; contract review from M2A.1 | local | Versioned serialization; no circular imports; incomplete/expired/ambiguous opportunities fail safely; operator fields present. | Recommendation is not placement; scope isolation. | Canonical/recommendation packages/tests | serialization/import/no-bleed/expiry tests; `make check` | Resolve final name and delivery seam deliberately. | Pending. | None yet. |
| M2F.2 Conservative opportunity ranking | TODO | CanaryView | Rank using dark reachability, sensitivity, fan-out, trust boundaries, unused paths, weak visibility, identity confidence, and history. | Placement should maximize signal while communicating uncertainty. | M2F.1 | DGX Kubernetes end-to-end | Each factor has evidence/provenance; missing data lowers confidence; bounded deterministic ranking; no placement side effect. | Learned parameter rule where applicable; no auto-enforce. | Recommendation/ranking/tests | golden/property/bounds tests; DGX known opportunities | Defaults and calibration must be documented if learning is added. | Pending. | None yet. |
| M2F.3 Placement preview and approval handoff | TODO | Shared platform / CanarySting | Present ActionPlan and deliver only an approved opportunity to CanarySting's control plane. | Preserves human trust and clear ownership of materialization. | M2F.2, M2B.5 | DGX Kubernetes end-to-end | Preview shows target/change/traffic/blast radius/validation/rollback; approval is one confirmation; recommendation-to-approval ≤2 steps; unauthorized/changed plan rejected. | Same authority for human/agent; Sting decides safe materialization. | Dashboard/action seam/operator API/tests | auth/immutability/interaction Playwright; DGX dry/fixture handoff | No automatic placement initially. | Pending. | None yet. |
| M2F.4 Placement result feedback | TODO | CanaryView / CanarySting | Record placement, validation, touchability, cleanup, expiry, failure, and rollback as CanaryView evidence. | Closes the View→Sting→View loop. | M2F.3 | DGX Kubernetes end-to-end | State truthful/idempotent; evidence/provenance/lifecycle linked to approved plan; actual canary secret never retained; failed/removed placement not shown active; deletion/expiry propagation and cleanup verified. | Bounded harmless canary; audit; privacy. | Operator/Sting events, View collector, UI/tests | lifecycle/retry/secret-negative tests; DGX placement/control/cleanup | Does not yet authorize punitive response. | Pending. | None yet. |

## Milestone 3 implementation inventory: Kubernetes Profile 3/4 reference slice

Goal: preserve and implement the Kubernetes M3D/M3E reference path—workload → Cilium → Envoy → CanaryView correlation → CanarySting canary/touch/verdict/eBPF → CanaryView trace/audit/console—without making it the default adoption path. M3A–M3C lower-footprint work is governed by the post-meeting roadmap authority and must precede or justify this profile in executable planning.

| Task | Status | Product area | Objective | Why it matters | Dependencies | Required validation tier | Acceptance criteria | Relevant architecture constraints | Likely files | Validation commands | Implementation notes | Completion evidence | Follow-up work discovered |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| M3.1 Deployable datapath/control-plane assets | TODO | CanarySting | Add deterministic CRD install, DaemonSet, operator Deployment, ServiceAccounts, and least-privilege RBAC. | The first vertical slice needs reproducible node-local deployment. | M2F.4, M1D.1 | DGX Kubernetes end-to-end | ARM64 assets deploy declaratively; verbs/capabilities/host mounts minimal; image/artifact provenance; apply twice/delete/cleanup pass. | Per-node engine; no sidecar; no cluster-admin default. | Deploy manifests, operator, DGX deploy/cleanup | render/validation/RBAC tests; DGX pre/apply/health/delete/post | Product assets distinct from test fixtures. | Pending. | None yet. |
| M3.2 Live Kubernetes/mesh identity ingestion | TODO | CanaryView / CanarySting | Ingest namespace, service account, pod, workload owner, selected labels, and verified mesh evidence. | Identity is the spine of scope and correlation. | M3.1, M2A.3 | DGX Kubernetes end-to-end | Stable allowlisted labels; churn handled; mesh proof preferred; parse-only SPIFFE never verified; source updates/deletes correct. | Mesh-first; labels lower confidence; read-only ingestion. | `internal/identity`, `internal/k8s`, RBAC/tests | informer/fake/forged tests; DGX relabel/restart/mesh/fallback | Do not bulk ingest sensitive labels. | Pending. | None yet. |
| M3.3 Workload scope and forged-scope defenses | TODO | CanarySting / Shared platform | Derive trusted cluster/namespace/workload scope and enforce engine-side re-resolution for submit/outcome paths. | Prevents cross-deployment learned-state or action bleed. | M3.2 | DGX Kubernetes end-to-end | Ambiguous/missing identity fails closed; malicious caller scope cannot write/read/act across scope; cookie-bound outcome preserved. | Rule 5; engine authority; no global fallback. | Identity/scopemap/engine/boot/gRPC/tests | adversarial/no-bleed/persistence tests; DGX cross-namespace | Context augments, never replaces, the cookie join. | Pending. | None yet. |
| M3.4 Full idempotent DeceptionPolicy reconciliation | TODO | CanarySting | Implement desired-state calculation, actual-state observation, idempotent lifecycle, workload/namespace/service-account/owner/label selectors, and envtest coverage. | Safe placement requires deterministic control-plane convergence. | M3.3 | DGX Kubernetes end-to-end | Create/update/delete/retry/conflict/restart converge; unsafe broad/empty selection rejected; ownership/finalizers/status truthful; envtest and K3s smoke pass. | Operator has no scoring logic; bounded/precise ownership; rule 11 recommendations advisory. | Operator API/controller/desired state/tests/CRD | unit/race/envtest/generated drift; DGX apply twice/restart/delete | Missing envtest assets fail; no implicit downloads. | Pending. | None yet. |
| M3.5 Approved opportunity to harmless placement | TODO | CanarySting / CanaryView | Select one fixture and materialize one approved CanaryOpportunity through DeceptionPolicy. | Connects intelligence to safe deception. | M3.4, M2F.3 | DGX Kubernetes end-to-end | Exactly one target; harmless/bounded canary; idempotent placement; View records lifecycle; expiration/removal succeeds. | Human approval; Sting owns materialization; no automatic placement. | Operator/canary/View evidence/DGX fixture | selector/approval tests; DGX placement/touchability/control/cleanup | Narrowest useful canary type first. | Pending. | None yet. |
| M3.6 Correlated touch-to-response vertical slice | TODO | CanaryPlatform | Exercise real flow, observations, canary touch, View adversarial evidence, Sting verdict, precise eBPF response, View trace, audit, and console. | Proves the complete platform loop. | M3.5, M2D.4, M1D.1 | DGX Kubernetes end-to-end | Hop-by-hop trace/evidence; intended target response; unrelated traffic healthy; audit/UI/action state agree; explicit PASS/FAIL; repeatable cleanup. | Cookie join; canary-only punitive trigger; bounded precise response; baseline maturity/operator floor. | All vertical-slice seams and DGX harness | local gates; pre/scenario/collect/correlate/target-control/cleanup/post; Playwright | Preserve the same user context across View and Sting. | Pending. | None yet. |
| M3.7 No-touch restraint and restoration proof | TODO | CanarySting / Shared platform | Prove anomaly/dark reachability/recommendation without canary touch creates no punitive response and cleanup restores baseline. | Makes the non-negotiable false-positive guardrail visible. | M3.6 | DGX Kubernetes end-to-end | Zero enforcement/action state before touch; recommendation stays advisory; cleanup twice; Cilium/K3s/BPF/connectivity and bystanders healthy after. | Rules 8/11; observe before enforce. | Engine/graph/operator/DGX tests/UI | zero-base/no-touch tests; BPF/policy/audit inventories; post-health; operator workflow assertion | Failure blocks M3 completion. | Pending. | None yet. |

## Milestone 4: CanaryView human interface

Goal: expand the first real-trace slice into the accessible unified CanaryPlatform console. M4 is an expansion, not the start of UI work.

| Task | Status | Product area | Objective | Why it matters | Dependencies | Required validation tier | Acceptance criteria | Relevant architecture constraints | Likely files | Validation commands | Implementation notes | Completion evidence | Follow-up work discovered |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| M4.1 Unified console shell and Operations | TODO | Shared platform | Implement Operations/Incidents/Flows/Canaries/Actions/Integrations navigation and context preservation. | One product experience must replace subsystem/vendor navigation. | M3.7, M2B.5 | DGX Kubernetes end-to-end | Accessible navigation; deep-link/back context; Operations surfaces cases, coverage, actions; current routes migrate without losing useful views. | Unified console; progressive disclosure. | Dashboard app/backend/tests | frontend lint/build; Playwright keyboard/navigation; DGX smoke | No mass UI rename before projections exist. | Pending. | None yet. |
| M4.2 Incident workspace | TODO | CanaryView | Implement SecurityCase summary, affected scope, controls, timeline, explanation, impact, recommendation, action, and containment. | Operators need all five decision questions in one place. | M4.1 | DGX Kubernetes end-to-end | Summary→explanation/recommendation 0–1 click; evidence one click; observed vs potential and confidence clear; partial/conflicting fixture passes. | Explanation/recommendation contracts; AI transparency. | Dashboard/API/fixtures/tests | unit/API; Playwright click-budget/a11y; DGX real case | Plain language leads; technical IDs disclosed beneath. | Pending. | None yet. |
| M4.3 Flow/trace and topology workspace | TODO | CanaryView | Show correlated hops, identities, translations, policy decisions, graph paths, gaps, conflicts, and evidence in one workspace. | Cross-tool security tracing is the core passive value. | M4.2, M2E.6 | DGX Kubernetes end-to-end | Complete trace accessible in workspace; graph has table alternative; why-allowed/source-disagreement questions require no query/prompt. | Evidence adjacent; vendor provenance; accessibility. | Dashboard flow/topology/API/tests | fixture Playwright; keyboard/screen-reader checks; DGX trace | Preserve current useful flow/topology drilldowns. | Pending. | None yet. |
| M4.4 Canaries workspace | TODO | CanaryView / CanarySting | Show opportunities, previews, placements, touches, signal value, expiry, cleanup, and result evidence. | View and Sting must feel continuous to the operator. | M4.2, M2F.4 | DGX Kubernetes end-to-end | Recommendation→approved placement ≤2 steps; no auto-placement; state truthful; rollback/cleanup visible; fixture and DGX path pass. | Human approval; harmless/bounded placement. | Dashboard/operator/API/tests | click-budget Playwright; lifecycle tests; DGX placement path | Keep recommendation beside explanation/evidence. | Pending. | None yet. |
| M4.5 Actions workspace | TODO | Shared platform | Show recommendation, immutable preview, approval, execution, validation, rollback, and audit states. | Safe action needs transparent control and recovery. | M4.2, M3.6 | DGX Kubernetes end-to-end | Preview one click; approval one confirmation; completed→rollback 1–2 clicks; stale plan reapproval; unavailable rollback explained. | No hidden agent authority; recommendation != action. | Dashboard/action API/audit/tests | state-machine/unit/auth tests; Playwright budgets; DGX safe action | Cross-vendor execution waits for M7. | Pending. | None yet. |
| M4.6 Integrations shell, accessibility, and usability validation | TODO | Shared platform | Add the generic, fixture-backed Integrations onboarding/health/capability workflow and validate WCAG-oriented core workflows with representative operators. | Trust depends on understandable coverage and accessible controls before source-specific productization. | M4.3–M4.5 | DGX Kubernetes end-to-end | Generic permissions/read-only/action distinction, freshness/gaps/errors/revoke states visible from fixtures; WCAG 2.2 AA target; keyboard/table alternatives; adoption metrics instrumented; no connector support claim. | No secret display; vendor provenance not navigation; M6 binds certified sources into this shell. | Dashboard/integration API/fixtures/tests/docs | lint/build; fixture Playwright/accessibility; usability protocol; DGX fixture source-health smoke | No new dependency install without approval. | Pending. | None yet. |
| M4.7 Retention, legal-hold, and model-use workflow | TODO | Shared platform / CanaryView | Implement Lean/Standard/Regulated selection, consequence estimates, lifecycle visibility, legal holds, and separate model-use authorization. | Click-ops users need understandable privacy, storage, cost, and model controls without policy code. | M4.1, M4.6, M2A.0.4 | DGX Kubernetes end-to-end | Standard recommended; class/period/volume/cost/sensitive fields/expiry/hold/model/region visible; advanced override one screen; hold and model-use permissions separate; source deletion/broken-reference effects visible; fixture Playwright and DGX lifecycle smoke pass. | No YAML/query/CLI; retention != model use; least privilege; no secret values. | Dashboard/lifecycle API/auth/audit/fixtures/tests | lifecycle/auth/API tests; frontend lint/build; fixture Playwright click/a11y; DGX expiration/reference smoke | Operator job: choose and audit lifecycle controls. No backend choice is implied by the UI. | Pending. | None yet. |

## Milestone 5: CanaryView agent interface

Goal: provide structured machine access to the same intelligence and authority boundaries as operators, starting read-only.

| Task | Status | Product area | Objective | Why it matters | Dependencies | Required validation tier | Acceptance criteria | Relevant architecture constraints | Likely files | Validation commands | Implementation notes | Completion evidence | Follow-up work discovered |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| M5.1 Read-only query API | TODO | CanaryView | Expose scoped `query_graph`, `get_trace`, `get_evidence`, `get_identity`, and `get_policy_decisions`. | Agents/tools need structured intelligence without scraping UI/raw stores. | M4.3, M4.7 | DGX Kubernetes end-to-end | Authenticated/authorized scope; bounded pagination/query cost; same evidence/confidence/lifecycle availability as UI; audit; no mutation or lifecycle/model-use bypass. | Rules 5/9; no hidden raw or authority path. | API/proto/transport/auth/tests | schema/compat/auth/no-bleed/bounds/lifecycle tests; DGX smoke | Human view projections may differ; semantics do not. | Pending. | None yet. |
| M5.2 Explanation and analysis operations | TODO | CanaryView | Add `explain_path`, `find_dark_reachability`, `find_attack_path`, and `find_canary_opportunities`. | Enables explainable automation and integration. | M5.1, M2F.2 | DGX Kubernetes end-to-end | Results cite evidence/provenance/confidence/missing/conflicts; deterministic results distinguish model text; bounded queries. | Advisory only; no auto-enforce. | Query/explanation APIs/tests | golden/property/auth/bounds tests; DGX known cases | No natural-language prompt is required. | Pending. | None yet. |
| M5.3 Agent trust and parity guard | TODO | Shared platform | Enforce that agents use the same approval/authorization/action contracts and cannot bypass UI-visible controls. | Machine access must not become a covert authority channel. | M5.2, M4.5 | DGX Kubernetes end-to-end | Structural/import/auth tests prove read-only surface; all attempted mutations denied/audited; evidence fields match console. | Human/non-human parity; AI transparency. | Authz/API/audit/tests | adversarial auth/hidden-route scans; DGX denial proof | Later recommendations use normal Recommendation objects. | Pending. | None yet. |

## Milestone 6: CanaryView connector ecosystem

Goal: expand CanaryView from the proven DGX/local correlation stack into a vendor-neutral enterprise intelligence plane. Category contracts, the collector/capability/action separation, waves, certification requirements, and open questions are governed by `docs/CANARYVIEW_CONNECTOR_ARCHITECTURE.md`.

| Task | Status | Product area | Objective | Why it matters | Dependencies | Required validation tier | Acceptance criteria | Relevant architecture constraints | Likely files | Validation commands | Implementation notes | Completion evidence | Follow-up work discovered |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| M6A Connector framework and first certified lab sources (Wave 0) | TODO | CanaryView / Shared platform | Productize the existing M2B collector seam; evaluate CanarySting, kernel/eBPF, Kubernetes, Cilium, Hubble, Envoy, NGINX, and OpenTelemetry when present as candidates; promote only independently certified sources into reference connector contracts; add capability manifest, extension/evidence envelope, health/coverage, schema drift, replay/backfill, fixtures, M4.6 bindings, and permission review. | A category-first reference implementation prevents every vendor integration from inventing intake, health, authority, and support semantics without overstating candidate maturity. | M5.3, M4.6, M2B.5 | DGX integration | At least one streaming and one polling/backfill source independently pass certification; each promoted source has a current manifest and health/capability visible in the existing Integrations shell and agent interface; provenance/confidence/raw references preserved; duplicate/reordered/delayed/partial events tested; no write permission required; all unpromoted candidates remain explicitly unsupported/unimplemented. | Extend M2B and bind M4.6—no duplicate collector/UI/capability abstraction or alternate observation path; read-only default; vendor extensions stay evidence; lifecycle rules apply. | CanaryView collector/capability/health contracts, reference adapters, fixtures, Integrations/API bindings, docs/tests | schema/contract/dedupe/order/delay/partial/replay/backfill/rate-limit/permission/no-bleed/redaction/drift/upgrade/read-only tests; local gates; applicable DGX source proofs | This is Wave 0 and does not reset/replay M2B, promote the candidate set as a whole, or conflate a CanaryView connector with the NGINX/Envoy CanarySting proxy adapter. The current NGINX adapter remains a stub and no NGINX collector is implied. | Pending. | None yet. |
| M6B Wave 1 — cross-vendor proof | TODO | CanaryView | Add read-only evidence in priority order: (1) Palo Alto Networks PAN-OS/Panorama, Prisma Access when partner-accessible, and Strata Logging Service; (2) Cloudflare Logpush and licensed edge/WAAP context; (3) Microsoft Defender XDR/Endpoint plus Entra ID or CrowdStrike Falcon plus the available identity provider; (4) AWS flow, audit, EKS, threat, load-balancer, and resource identity sources. | Firewall/NAT, edge, workload/runtime, endpoint/identity, cloud, and CanarySting evidence close the largest end-to-end trace gaps and prove competing vendor families. | M6A | DGX attacker/correlation plus authorized licensed environments | One mixed-vendor trace joins edge, firewall/translation, workload/runtime, endpoint/identity, and CanarySting evidence; at least three independent vendor families; every join explainable; missing/conflicting evidence, health, and latency visible; no vendor-specific canonical assumptions. | Collector-only/read-only; native control planes authoritative; least privilege; current official docs plus licensed-environment verification required; manifests retain only sanitized non-identifying attestations while detailed evidence remains protected and deployment scoped; future actions remain M7. | Wave 1 collectors, manifests, fixtures, evaluator, console/docs/tests | complete connector certification matrix; cross-trace precision/completeness/translation/identity/conflict measures; Playwright; DGX/local sources; separately authorized vendor lab proofs | Design-partner access chooses the endpoint/identity option and may reorder within the wave, never bypass architecture gates. Candidate future M7 actions are temporary Palo Alto rule/object/tag changes, scoped Cloudflare controls, verified endpoint containment/release or supported identity actions, and narrow cloud-native containment. | Pending. | None yet. |
| M6C Wave 2 — enterprise breadth | TODO | CanaryView | Add Akamai; Imperva database/sensitive-data evidence first and edge/API evidence second; the unselected Microsoft Defender or CrowdStrike platform; Fortinet; Zscaler; and Okta under their category contracts. | Multiple vendors per category prove that the canonical model is not shaped around one product and extend traces to sensitive data. | M6B | DGX attacker/correlation plus authorized licensed environments | Category contracts support multiple vendors without one-off canonical fields; at least two edge/WAAP vendors; at least two firewall/SASE vendors; at least two endpoint/identity combinations; one trace reaches a sensitive data asset or database-security observation; manifests and certification evidence current. | Vendor fields stay in evidence envelope; licensed/versioned behavior verified before support claims; no write credentials or policies. | Wave 2 collectors/manifests/fixtures/evaluator/console/docs/tests | certification matrix per connector; multi-vendor category contract/golden/no-bleed/drift/upgrade/trace tests; authorized vendor labs | Imperva requires licensed API documentation and a test/design-partner tenant. Design-partner pull may reorder only within the wave. | Pending. | None yet. |
| M6D Wave 3 — cloud, posture, operations, and ecosystem expansion | TODO | CanaryView / Shared platform | Add Microsoft Azure, Google Cloud, Check Point, Prisma Cloud, Wiz, Sysdig, CyberArk, Splunk, Elastic, Microsoft Sentinel, Google Security Operations, ServiceNow, and Datadog/Grafana where useful evidence exists; publish cases/traces/explanations/recommendations into existing SOC workflows. | Broadens cloud/workload/privileged-access/posture context and meets operators in existing workflows without copying every raw event. | M6C | DGX attacker/correlation plus authorized licensed environments | New sources satisfy category/manifest/certification gates; historical source references remain usable/explicitly unavailable without bulk raw copying; destination projections preserve CanaryView semantics/provenance; SIEM/SOAR/ITSM products do not define the canonical model. | Federated evidence; vendor-native authority; destination is not canonical truth; regional/licensing constraints explicit. | Wave 3 connectors/destinations/manifests/fixtures/console/docs/tests | source and destination contract/redaction/reference/compatibility tests; trace/case projection tests; Playwright; authorized environment proofs | Roadmap presence is not an API/edition/region promise; implementation-time verification is mandatory. | Pending. | None yet. |
| M6E Connector operations and marketplace readiness | TODO | Shared platform / CanaryView | Harden the existing M4.6 Integrations shell and M6A connector registry across the certified M6 portfolio: permission review, connection tests, health/lag, schema drift, versioning/upgrades, replay/backfill, volume/retention estimates, region/license constraints, certification kit, and support labels. | Certified connectors require an understandable and operable portfolio lifecycle, not merely successful authentication or a second operations surface. | M6D, M4.6 | DGX attacker/correlation plus applicable authorized environments | Operator can answer visibility, gaps, currency, permissions, future actions, and current read/write authority on one screen; rotate/disconnect safely; capability available to agents; fixture/certification kit covers all required failure modes; support status requires per-connector wave exit and licensed evidence. | Click-ops; no secret display; separate write authority; health/capability are product data; no connector supported from authentication alone; reuse M4.6/M6A UI and registry. | Existing Integrations UI/API and connector registry/operations, fixtures/certification/docs/tests | Playwright onboarding/health/rotation/disconnect/a11y; certification matrix; upgrade/replay/backfill/volume/retention tests; local gates and applicable source proofs | Hardens M4.6 and M6A across the certified portfolio; it does not create a second UI, registry, or onboarding workflow. | Pending. | None yet. |

## Milestone 7: Cross-vendor action layer

Goal: express a vendor-neutral `SecurityIntent`, translate it into one or more vendor-native `ActionPlan` objects, and preview, authorize, execute, verify, expire, audit, and roll back controlled actions while native products retain their control planes.

| Task | Status | Product area | Objective | Why it matters | Dependencies | Required validation tier | Acceptance criteria | Relevant architecture constraints | Likely files | Validation commands | Implementation notes | Completion evidence | Follow-up work discovered |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| M7.1 SecurityIntent and authorization model | TODO | Shared platform | Finalize Recommendation → SecurityIntent → vendor-native ActionPlan, capability checks, approval/delegation, expiry, idempotency, conflict, rollback-order, and authorization policy. | No cross-vendor mutation is safe without one visible intent and authority model that preserves native semantics. | M6E, M5.3 | local | Human approval initially; agents cannot bypass; read/write principals separate; manifest capability required; scope/action/control-plane least privilege; immutable intent/plan/version and audit semantics; partial-failure states defined. | Recommendation/intent != action; no hidden authority; rule 11; native control planes authoritative. | Architecture/contracts/authz/audit/tests | state/property/adversarial auth/capability-conflict tests; review | Resolve multi-control conflict and rollback-order questions before execution. | Pending. | None yet. |
| M7.2 Vendor-native planning, simulation, and preview | TODO | CanaryView | Translate one SecurityIntent into one or more native ActionPlans and estimate exact changes, preserved traffic, affected scope, impact, permissions, validation, expiration, and rollback before approval. | Operators need one coordinated view without a false generic policy abstraction. | M7.1, M2E.5 | DGX Kubernetes end-to-end | Unsupported/unknown capability explicit; stale manifest/source invalidates preview; each plan names adapter/native action/permissions; no write credential needed for preview where supported; evidence and coordination/rollback order attached; interaction budget passes. | No side effect; never claim absent manifest capability; reversible/expiring actions preferred. | SecurityIntent/planner/simulation/action plan/API/UI/tests | deterministic fixtures; no-write/capability/stale-plan guards; Playwright; DGX dry proof | Example intent: prevent `checkout-api` reaching `vault-proxy` for 30 minutes while preserving customer-facing traffic. | Pending. | None yet. |
| M7.3 CanarySting and Cilium reference Action Adapters | TODO | CanarySting / Shared platform | Execute approved precise CanarySting and temporary Cilium-native plans through separately authorized adapters. | Proves the action seam against available DGX controls before external vendor mutation. | M7.2, M3.7 | DGX Kubernetes end-to-end | Exact approved plan only; separate write authority; before-state/revalidation; native change ID; target/control proof; verify; expiry and verified removal; rollback/audit/cleanup; partial failure/health visible. | Touch-only punitive Sting; Cilium remains native authority; human approval; placement distinct from punitive response. | Action adapters/operator/DGX scripts/tests | auth/plan/capability mismatch tests; DGX plan/preview/approve/apply/verify/expire/rollback/post | Reference adapters do not broaden CanarySting's trigger or silently manage Cilium. | Pending. | None yet. |
| M7.4 Enterprise vendor-native Action Adapters | TODO | Shared platform | Add only manifest-declared, implementation-time-verified adapters such as temporary Palo Alto rule/object/tag changes, scoped Cloudflare WAF/rate-limit changes, endpoint containment/release, and narrow cloud-native containment. | Extends coordinated response across categories without replacing vendor control planes. | M7.3, M6E | DGX integration plus separately authorized licensed vendor environments | Per-control least privilege and separate write credentials; exact approval; native change ID; applicable plan/preview/scope/impact/apply/verify/expire/rollback/audit support; fixture plus licensed live evidence; unsupported capabilities rejected; partial failures represented. | Native authority retained; passive onboarding unchanged; official docs/edition/region/license assumptions recorded. | Vendor Action Adapters/manifests/tests/runbooks | fakes/fixtures/auth/preview/apply/idempotency/verify/expiration/rollback/integration tests; separately authorized sandboxes | Each live adapter requires separate external authorization; collector support never implies action support. | Pending. | None yet. |
| M7.5 Multi-adapter execution, expiration, rollback, and audit UX | TODO | Shared platform | Coordinate approved native plans and present one execution with per-adapter apply, validation, expiration, verified removal, rollback order, and evidence. | Operators need trustworthy recovery and honest partial state across several controls. | M7.3, M7.4 | DGX integration plus applicable authorized environments | Requested/applied/verified/partially failed/expired/removal-verified/rollback-pending/rolled-back distinct; vendor change IDs and per-plan provenance visible; partial outcomes and conflicts explicit; plan-to-approval and rollback budgets pass; audit immutable. | Same human/agent contract; evidence adjacent; no action ends in an unexplained dead end. | Action coordinator/service/dashboard/audit/tests | state-machine/partial-failure/retry/expiry/removal/rollback-order tests; Playwright; DGX and authorized integration scenarios | Operator sees one unified plan without losing native adapter state or authority boundaries. | Pending. | None yet. |

## Milestone 8: Production hardening backlog

Track these carried-forward items without pulling them ahead of their dependencies or explicit approval.

| Task | Status | Product area | Objective | Why it matters | Dependencies | Required validation tier | Acceptance criteria | Relevant architecture constraints | Likely files | Validation commands | Implementation notes | Completion evidence | Follow-up work discovered |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| M8.1 Console and raw-tap authentication | TODO | Shared platform | Authenticate/authorize dashboard, raw tap, case/evidence, lifecycle, and action views. | Views/events expose sensitive topology and state. | M4.7, M7.1 | DGX Kubernetes end-to-end | Unauthenticated denied; scoped roles; raw, hold, model-use, and action permissions separated; stronger authorization and audit pass. | Privacy/least privilege; no hidden agent path. | Dashboard/tap/auth/manifests/tests | authn/authz/no-bleed/exposure/lifecycle tests; DGX | Explicit loopback dev mode only. | Pending. | None yet. |
| M8.2 Secure listeners, transport, and engine/intelligence authorization | TODO | Shared platform / CanarySting | Make adapter listeners safe, authorize engine operations/scopes, and authenticate cross-deployment intelligence. | Authenticated transport without operation/scope authorization is insufficient. | M8.1, M3.3 | DGX Kubernetes end-to-end | Routable plaintext/unauth default refused; mTLS/allowlist/replay/queue controls; raw payload rejection; default-deny scope authz. | Thin adapters; rules 5/9; safe local mode explicit. | Transport/gRPC/boot/intelligence/manifests/tests | bind/mTLS/authz/replay/privacy/DGX port tests | Transport does not become detection logic. | Pending. | None yet. |
| M8.3 Keyed audit and external witness | TODO | Shared platform | Require keyed production audit integrity and define an external witness strategy. | Local audit alone cannot prove total deletion. | M7.5, M8.2 | DGX Kubernetes end-to-end | Missing key refused outside dev; rotation/tamper/wrong-key; witness threat model/protocol/privacy/outage/replay validated. | Never expose keys; approved derived egress only. | Audit/persist/transport/manifests/docs/tests | integrity/rotation/protocol/privacy/DGX restart tests | External service mutation needs approval. | Pending. | None yet. |
| M8.4 Safe SIEM secrets and consolidated configuration | TODO | Shared platform | Remove secrets from argv/logs and consolidate validated config/precedence/profiles. | Hidden secret/config divergence is unsafe. | M8.2 | DGX Kubernetes end-to-end | Secret references/redaction/rotation; unknowns refused; no silent scope/tier/fail/exposure changes; migration documented. | Default-deny egress; secrets never committed. | SIEM/config/commands/manifests/tests | parse/precedence/redaction/argv/startup/DGX tests | Do not broaden destinations. | Pending. | None yet. |
| M8.5 Production storage, calibration, feedback, logging, and retention | TODO | CanarySting / CanaryView | Select/productize approved storage plus scoped feedback, bounded/redacted logs, lifecycle enforcement, and model registry behavior. | Learned parameters and evidence need safe durable operations after the architecture gate. | M8.2, M2A.4, M2A.0.4 | DGX Kubernetes end-to-end | Storage choice meets measured targets; one feedback loop/evidence floor; no bleed; rotation/redaction/load; profile expiry/hold/deletion/invalidation/residency/key/model-use/lineage behavior and cost verified. | Rule 7; privacy; bounded resources; graph view not sole truth. | Calibration/feedback/config/logging/storage/model registry/docs/tests | no-bleed/restart/redaction/load/rotation/retention/deletion/hold/model-use/DGX disk tests | Production backend selection requires explicit review and is not part of this bootstrap. | Pending. | None yet. |
| M8.6 Frontend security and workflow tests | TODO | Shared platform | Extend fixture-driven tests across auth, errors, live data, actions, rollback, accessibility, and interaction budgets. | Safety indicators and approvals must not regress. | M8.1, M7.5 | DGX Kubernetes end-to-end | Unit/component/Playwright deterministic in CI; auth/action/rollback/click budgets/a11y covered; real DGX smoke passes. | Truthful display; no prompt dependency. | Dashboard/CI/tests | lint/build/unit/Playwright/DGX smoke | Dependency installation requires approval. | Pending. | None yet. |
| M8.7 Cilium-aware exposure and workload network hardening | TODO | Development infrastructure | Address management/health listeners, NetworkPolicy, host firewall, and workload encryption with staged rollback. | Current lab exposure/policy/encryption posture is not production-ready. | M3.7, M2E.5, M8.2 | DGX Kubernetes end-to-end | Approved controls preserve SSH/API/health/required flows; policy audit first; firewall lockout prevention; encryption threat/perf/coexistence/rollback proven. | No implicit K3s/Cilium/firewall mutation; human sign-off/maturity. | Hardening docs/manifests/config/runbooks | listener/traffic/health/perf/rollback evidence | Security correction: each change needs separate explicit approval. | Pending. | None yet. |
| M8.8 Kubeconfig permissions and least privilege | TODO | Development infrastructure | Correct world-readable cluster-admin kubeconfig and replace routine sudo/cluster-admin with scoped rights. | This is the highest-priority known lab credential/privilege risk. | M1B.7; explicit user approval | DGX Kubernetes end-to-end | Approved owner/mode correction; no secret output; access/health/rollback; task-specific RBAC/capabilities allow required tests and deny unrelated operations. | Least privilege; avoid lockout; no credential exposure. | Environment evidence, RBAC/sudo/capability config/runbooks | stat/auth matrix/harness regression/denied ops/health | Security correction: do not bundle with ordinary development; explicit approval required. | Pending. | None yet. |
